# Adversarial review of the warrant-containment note — findings

Produced 2026-08-16 by six independent reviewers (prior art, method,
internal consistency, domain accuracy, a DARPA reader, and code-vs-note),
each finding then attacked by a separate skeptic instructed to refute it.
48 findings raised, **32 survived refutation**, 17 of them blocking.

THREE WERE RE-VERIFIED BY HAND before this file was written, and all three
hold. They are not stylistic:

* §3.4 quotes `C = 0.789`. No program prints it — `probe_criterion` returns
  1.000 for both configurations. The figure was invented.
* `probe_roots.build` accepts an `rnd` argument and never uses it, so the
  ten-seed variance run for A_crit measured one graph ten times. Its
  'constant across seeds' result is vacuous.
* §3.1 quotes 52,000 where the probe prints 59,716 — a figure carried over
  from an earlier run.

`inventory/note_claims.py` passed all three. It matches digit strings, not
meanings, and `0.789` was never in its list at all. A checker that gives
false assurance is worse than none, and it was written by the same hand it
was supposed to check.

**The note must not be deposited in its current state.**

## 1. [BLOCKING] §3.4 Evidence and authority are not interchangeable
*lens: defense*

**Quote.** evidence 100% genuinely redundant, authority not: losing the commander costs **C = 0.789**;

**Defect.** The code contradicts this. `probe_criterion.C(1.0, 0.0, 0.0, 0.0, 0)` — evidence fully redundant, authority not, target = the commander (node 0) — returns **1.000**, not 0.789. I ran it. The probe's own §1 prints both configurations as `C = 1.000`. The figure 0.789 comes from a different row entirely: `C(0.0, 0.0, 1.0, 0.0, 7)` — evidence NOT redundant, authority FULLY redundant, target = mid-level agent 7. The note has reversed the two dimensions AND swapped the target node, then built the section's headline contrast (0.789 vs 1.000) out of the mismatch. The true contrast is 1.000 vs 1.000, i.e. no contrast at all. `inventory/note_claims.py` never catches this: 0.789 appears in neither CLAIMS nor FIGURES, so the one headline number of §3.4 is the one number the claims checker does not demand back.

**Attack.** "Your abstract's third finding rests on a number your own program does not produce, in a configuration your own program contradicts. You told me a claims checker re-verifies every figure; it skips this one. I now have to assume every unchecked figure in the note is in the same state, and I have no cheap way to tell which those are. Withdraw and re-derive."

**Correction.** Three fixes, smallest first. **1. Correct the §3.4 bullets** (paper/warrant-containment-note.md:172-176). Replace: ``` - evidence 100% genuinely redundant, authority not: losing the commander costs **C = 0.789**; - both 100% genuinely redundant: the commander still costs **C = 1.000** — the entire collective. ``` with what the code prints: ``` - evidence 100% genuinely redundant, authority not: losing the commander costs **C = 1.000** — the entire collective; - adding 100% genuine authority redundancy on top does not move it: still **C = 1.000**, because every second warrant is drawn from an agent already downstream of the commander. ``` This is the honest reading and it is *stronger* than the printed version — perfect evidence redundancy buys not "a little" but literally nothing. The "not interchangeable" thesis then rests where it actually belongs, on the min-0.50 comparison in the next paragraph (0.03% vs 1.01%, factor 31), which is correct as written. Drop the word "still" from the second bullet, since there is no longer an escalation to signal. **2. Optionally recover the real contrast at a non-root target.** If §3.4 wants a two-number contrast, the honest one from the §2 table is at agent 7: evidence-redundant-only = 0.132, authority-redundant-only = 0.789. Note that this cuts the *opposite* way from the min-0.50 rows (at zero redundancy evidence matters more; at min 0.50 authority matters more) — so if it goes in, it must go in with that caveat, not as reinforcement. **3. Close the checker gap** in inventory/note_claims.py. probe_criterion.py currently has no FIGURES entry. Add one: ```python ("probe_criterion.py", ["1.000", "0.0101", "0.0003", "0.65", "0.35", "0.789", "0.132"]), ``` Better, and what the reviewer is really asking for: make the gap self-reporting rather than relying on someone remembering to list each figure. Add a pass that regexes every bolded numeric literal out of the note (`\*\*C? ?= ?([0-9.]+)\*\*` plus bare `**0.xxx**`) and fails on any that appears in no cached probe output. That converts "which figures are unchecked?" from an unanswerable question into a printed list, which is the property the note's §7 reproduction claim implicitly promises.

## 2. [BLOCKING] §3.2 Redundancy contains it, with a threshold that moves
*lens: defense*

**Quote.** Sweeping the share of such conclusions does not give a curve; it gives a collapse. ... below some redundancy one loss costs most of the collective, above it almost nothing, and there is very little in between to tune against.

**Defect.** The note prints three of the six columns the probe sweeps. `probe_topology.py` sweeps 0%, 50%, 75%, 85%, 90%, 95%. Full random-local row, which I ran: 95,041 → 47,344 → 20,840 → 2,470 → 56 → 4. That is a smooth monotone decay across four orders of magnitude with a well-populated middle. At 85% redundancy random-local loses 2.5% of the collective — precisely a tunable intermediate regime. Hierarchy: 95,081 → 9,575 → 194 → 31 → 10 → 8, also smooth. The note deletes the 50% and 85% columns — the two that show the curve — and then asserts there is no curve. `note_claims.py`'s FIGURES list for probe_topology is exactly the retained numbers (95,041 / 20,840 / 95,081 / 194 / 33,943), so the checker is structurally incapable of noticing the omission.

**Attack.** "I ran your probe. Your table is a three-point subsample of a six-point sweep, and the two points you dropped are the two that falsify the sentence underneath it. 2,470 at 85% is not 'very little in between.' Either this is selection to fit a narrative or you did not look at your own output — and neither is a thing I fund. The 'shape transfers' claim, which is the only claim you say generalises, is the claim the omitted data breaks."

**Correction.** Три правки; первая обязательна, третья закрывает дыру в чекере. 1) §3.2 — печатать весь свип (стоит одну ширину таблицы): | topology | 0% | 50% | 75% | 85% | 90% | 95% | threshold | |---|---|---|---|---|---|---|---| | random-local | 95,041 | 47,344 | 20,840 | 2,470 | 56 | 4 | **90%** | | hierarchy | 95,081 | 9,575 | 194 | 31 | 10 | 8 | **75%** | | scale-free | 33,943 | 2,228 | 139 | 54 | 20 | 8 | **75%** | (Если ширина критична — оставить три колонки, но подписать явно: "selected columns from a sweep of 0/50/75/85/90/95; full rows printed by `python3 db/probe_topology.py`". Это допустимый минимум, полная таблица лучше.) 2) Заменить предложение под таблицей. Убрать "does not give a curve; it gives a collapse" и "there is very little in between to tune against". Текст, который данные выдерживают: "Fallout falls roughly in proportion to the share of conclusions still resting on a single ground — until that share reaches about a quarter. Then it collapses: two to three orders of magnitude over the last ten to fifteen points of redundancy. What transfers is that the collapse exists and is sharp and sits near the top; where it sits moves by forty points — hierarchy and scale-free are already under 1% at 75% redundancy, random-local not until 90%. The transition is narrow, not absent, and the note is explicit about the difference: random-local at 85% still loses 2.5% of the collective, eight times better than the previous column and forty times worse than the next. A designer cannot take a constant off another system's report, and cannot assume the fall is a single step either." Заодно поправить §3.1→§3.2 связку: 20,840 на 75% — это 20.8%, поэтому формулировка "one loss costs most of the collective" ниже 75% для random-local неверна и должна уйти вместе с остальной фразой. 3) `inventory/note_claims.py` — сделать проверку двусторонней для таблиц-свипов. Минимум: дописать в FIGURES для probe_topology пропущенные ячейки — ["47,344", "2,470", "9,575", "31", "2,228", "54"] — тогда файл не позеленеет, пока их нет в заметке. Правильнее: добавить структурную проверку, что число колонок данных в таблице §3.2 равно `len` кортежа fracs в `probe_topology.threshold` (строка 117), импортировав его, а не хардкодя шесть. Это ровно тот класс ошибки, который FIGURES по построению пропускает, и он заслуживает строки в §3 «WHAT THIS DOES NOT CHECK» либо её сужения. 4) По культуре §5: это третья по счёту ошибка выбора статистики после уже записанной медианной. Одна строка в §5 («a table abridged to three of six swept columns, under a sentence asserting the omitted region was empty») стоит дешевле, чем позволить рецензенту найти это первым — тем более что §3.2 уже содержит признание про медиану и абзац читается как полный.

## 3. [BLOCKING] §7 Reproduction / Abstract
*lens: defense*

**Quote.** Public repository, one command each, no dependencies beyond the standard library

**Defect.** No URL. No commit hash. No release tag. No archive DOI for the code. No license statement. No seed values (the probes hardcode SEED = 20260816 and probe_variance uses 20260816 + k·7919; neither appears in the note). The actual remote is git@github.com:inventor1975/ZTL.git, discoverable only by having the working tree. The note also says "no dependencies beyond the standard library" while §7 asserts `run_all.py` "runs 115 stands and the Lean corpus" — the Lean corpus requires a Lean 4 toolchain, which is not the standard library. "115 stands" is never defined. This is a document being deposited with a DOI and cited in two external venues.

**Attack.** "You are asking me to cite this in a program document. I cannot locate the artifact, cannot pin the version I would be citing, and cannot tell whether the numbers move between commits — and your git log shows the paper section on related work landed one commit before I read this. Reproducibility claims without a resolvable, pinned, licensed artifact are marketing. This is the point where I stop reading."

**Correction.** Two edits, both mechanical; plus one thing only the curator can do. 1. Replace §7's opening and closing lines. Current: "Public repository, one command each, no dependencies beyond the standard library:" ... "`run_all.py` runs 115 stands and the Lean corpus, and asserts the zero-axiom line." Proposed: Repository: https://github.com/inventor1975/ZTL (MIT), branch `master`, pinned at commit <FULL-40-CHAR-SHA> / tag `warrant-note-v1.0`. Archived at DOI <Zenodo software DOI> [curator step]. Every probe below needs Python 3 and the standard library only, runs from the repository root, and is deterministic: the containment probes fix SEED = 20260816, and probe_variance.py sweeps SEEDS = [20260816 + k*7919 for k in range(10)]. [code block — ADD the missing line: python3 db/probe_variance.py, which produces the §3.7 table. Consider adding db/probe_sheet.py or say why it is out of scope.] `run_all.py` re-runs the whole regression corpus — 115 "stands", i.e. 115 independent check scripts, each asserting named figures from this note — and then builds the Lean corpus and asserts the zero-axiom line. This one command is the exception to the stdlib claim: it requires a Lean 4 toolchain (leanprover/lean4:v4.29.1 via elan). `python3 run_all.py --no-lean` runs the 115 stands without it. Install steps: REPRODUCE.md. 2. Fix REPRODUCE.md before anyone follows the pointer: it pins 9d9a07a (221 commits stale) and asserts "59 stands / 371 theorems / 21 modules" against a current 115. Re-pin to the deposit commit and re-derive all three numbers from a live run — do not hand-edit 59 to 115; the theorem and module counts need re-measuring too. 3. Curator-only, per the project contract: cut the tag, push, and upload the software archive to Zenodo to mint the code DOI. Claude prepares; the curator publishes. Do not deposit the note with the DOI slot empty — an unresolvable "public repository" is the exact claim the reviewer stops reading at, and this note's whole rhetorical position is that unbacked confidence is the defect it exists to catch.

## 4. [BLOCKING] Whole note; §6 Limits
*lens: defense*

**Quote.** **These are simulations on synthetic graphs.** ... No result here has been validated against a deployed system.

**Defect.** The note never runs on a single real dependency graph, and never explains why not. Real, free, downloadable dependency graphs at exactly this scale exist and would have taken a day: Debian/npm/PyPI package graphs, SBOM/SPDX graphs, Linux kernel module graphs, Maven Central, service-mesh call graphs. The three generators used (random-local, an 8-ary tree, preferential attachment) are textbook synthetic families with no calibration to anything. Consequently the note establishes that the phenomenon exists in three random-graph generators and nowhere else — including §3.3's headline, quoted in the abstract, that a quarter fake redundancy destroys containment. The disclaimer in §6 is honest but does not substitute for the missing experiment, and the abstract carries none of it: "a quarter of it being fake is enough to destroy containment" reads as a fact about collectives.

**Attack.** "Why is there not one real graph in here? Not a classified one — a public SBOM. You had the code, the scale, and a week. Either you tried and the effect did not survive contact with a real degree distribution, or it did not occur to you to try. From where I sit those look identical, and the second is worse. Until a real graph appears, §3 measures your generator, and I have no reason to believe your generator."

**Correction.** Three changes, in ascending cost. 1. Fix the abstract's "quarter" (free, and the sharpest actual inconsistency). "A quarter of it being fake is enough to destroy containment" is a threshold LOCATION, which §3.2 says does not transfer. Either qualify it — "on the command hierarchy measured here, a quarter" — or replace it with the transferable form the model actually licenses: "declared redundancy can be substantially fake with the report not moving at all." §3.4/§5a already do this correctly (q* median 0.35, range 0.30-0.45, "a property of this model, not a reassurance"); the abstract should not be looser than the body. 2. Say WHY, in §6 (one paragraph, free). Replace the bare "These are simulations on synthetic graphs" with a stated reason: public dependency corpora (npm, Debian, PyPI, Maven, SPDX SBOMs) record conjunctive dependency edges only — no declared independence, no authority dimension, no shared-origin annotation, no observed-versus-actual distinction — so they can supply a degree distribution but not the object under study; the warrant semantics would remain a synthetic overlay on a real skeleton. That converts an unexplained absence into a defended choice, which is what the hostile question is actually asking for. Add the honest remainder: no real degree distribution has been tried, so topology sensitivity is measured across three invented shapes and the range 75-90% may not bound a real one. 3. Do the cheap version anyway (about a day, low risk, high credibility). Add a fourth generator to /media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_topology.py returning the same (par, rev) pair from a downloaded package-dependency edge list — npm or Debian, free, ~10^5 nodes / 10^6 edges, the exact scale already used — overlay the same redundancy dressing via redundant() and re-run threshold(). Add the row to §3.2's table. Note that the note's own conclusion is robust to the outcome: a real graph whose threshold falls outside 75-90% CONFIRMS "the number does not transfer," and one that falls inside strengthens the range. The only claim at risk is "a threshold exists," which is a generic percolation property and is already hedged as "probably robust." Given that asymmetry, not running it is the more expensive choice. If it is not run, §6 must say so in those words rather than leaving the reader to infer which of the reviewer's two explanations applies.

## 5. [BLOCKING] §2, completeness
*lens: domain*

**Quote.** Completeness, the assertion ranked hardest for payables, is answerable by **no ledger whatever**: the invoice nobody entered leaves no row to carry a warrant.

**Defect.** This is not defensible to an auditor. It is true that a population cannot certify its own completeness, but that is not what the sentence says. Completeness for payables is never tested from the AP ledger; it is tested by reconciling the AP population against *other* ledgers — subsequent cash disbursements, unmatched goods-received notes / open receiving-report file, open purchase orders, vendor statement reconciliations, pre-numbered document sequence checks, three-way match. Every one of those is a ledger, and each detects rows-that-should-be-there from rows-that-are. The claim is also internally contradicted by the very probe it is drawn from: `db/probe_assertions.py:83` marks 'review subsequent invoices for payables wrongly in or out' as PLAIN — an ordinary SQL query — and that procedure *is* the search for unrecorded liabilities, i.e. a completeness procedure. The note therefore says in one sentence that completeness is answerable by no ledger and in the adjacent count that a completeness procedure is answerable by plain SQL. Underneath sits a category error: the argument assumes evidence must be per-row, whereas audit evidence for completeness is a claim about a *set*, supported by a procedure. In the note's own vocabulary, 'this population is complete' is a first-class claim resting on a withdrawable ground (`earned:reconciliation-2026-06`), which is exactly the shape the instrument exists to carry.

**Attack.** A hostile auditor: 'You have conceded the assertion the whole audit exists for, on an argument that shows you have not read an AP audit programme. Nobody tests payables completeness from the payables ledger — that is the first thing you are taught. And you have thrown away your own strongest case: a completeness assertion is a population-level claim with a named ground, and retracting the reconciliation that grounds it is precisely a cascade. This paragraph is the one place a working auditor stops reading.'

**Correction.** Two edits, prose and probe, plus one assertion to re-check. A. /media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md:86-88. Replace: "Completeness, the assertion ranked hardest for payables, is answerable by **no ledger whatever**: the invoice nobody entered leaves no row to carry a warrant." with something on this shape (wording the curator's): "Completeness, the assertion ranked hardest for payables, is where the warrant column adds least — but not for the reason it is tempting to give. Completeness is not tested from the payables ledger; it is tested against *other* files — subsequent cash disbursements, unmatched receiving reports, open purchase orders, pre-numbered document sequences, vendor statements — and most of those are ordinary joins. Two of the eight procedures below are exactly that, and are scored PLAIN. What no ledger reaches is the residue: a liability that left no trace in any system the auditor holds, and there is no row to carry a warrant. The assertion *about* the population — 'this set is complete as of the June reconciliation' — is itself a claim on a withdrawable ground, and that one the instrument does carry: retract the reconciliation and everything resting on the completed population moves." That concedes the true thing, drops the false one, and takes back the strongest case rather than giving it away. B. /media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_assertions.py:157-161. The printed paragraph repeats the overclaim twelve lines under its own counterexamples. Rewrite it to match A: name the two PLAIN completeness procedures (lines 75-76 and 84) explicitly, then state the residue claim narrowly. Also fix the two "why" strings at lines 38-41 and 61-62 — "no database can touch it" is the same overclaim; make it "no database certifies it from the population itself; the reconciliation procedures against other files are ordinary joins, and what is left over is the trace-free liability." C. Decide the verdict deliberately, don't leave it inherited. Either keep NEITHER for the two Completeness assertions but say in the "why" that it scores the residue and not the reconciliation procedures — or split the verdict and let the counts move. If the counts move, line 218's `assert c["NEITHER"] == 2` and the "5 of 20" figure in the note (warrant-containment-note.md:84-85) both need updating together. Keeping NEITHER with an honest justification is the cheaper and, I think, the more defensible option. D. Optional but strong: cite the completeness-attestation construction already in downstream/integration-slice-001/CURRENTNESS-TO-RELIANCE-...-v0.2.md:67 as the worked instance of the population-level claim. It turns the paragraph a working auditor currently stops at into the one that shows the instrument doing the job.

## 6. [BLOCKING] §2, all three scores (5 of 20; 2/2/4 of 8) vs Abstract
*lens: domain*

**Quote.** Scored against the profession's own list rather than ours — the twelve ISA 315 assertions and eight accounts-payable procedures — the score falls to **5 of 20** needing a warrant at all. … Against documented failures the instrument scores **2 TOUCHES, 2 EXPOSES, 4 OUT** of eight

**Defect.** Neither score is a measurement. In `probe_assertions.py:33-87` the PLAIN/WARRANT/NEITHER verdict is a hardcoded string the author typed beside each assertion; in `probe_failures.py:32-103` the TOUCHES/EXPOSES/OUT verdict is likewise a literal in a Python list. The 'program' does `Counter(...)` over the author's own opinions. The list is the profession's; the scoring is entirely the author's, and the sentence 'Scored against the profession's own list rather than ours' is built to transfer the authority of the list onto the scores. `inventory/note_claims.py:32-33` then 'verifies' these by grepping the output for the substring `TOUCHES 2 of 8` — i.e. it confirms the author's labels are still the author's labels. §5's defence ('Each is pinned by an assertion in the source so it cannot be quietly tuned away') is `assert c["OUT"] >= 4 and c["TOUCHES"] >= 2` at `probe_failures.py:146`, which pins a self-assessment, not a result. Only `probe_system_errors.py` actually executes the instrument. Meanwhile the Abstract states 'Every number below is printed by a program in a public repository and re-checked by a regression suite of 115 stands', which invites the reader to give §2's numbers the same standing as §3's simulation outputs. `probe_lattice.py:52` itself admits 'The grading … is the part a real accountant would correct' — no accountant has, and the note does not say so.

**Attack.** '§3's figures come out of a simulation; §2's come out of a Counter over a list literal. Both arrive under the same warrant — "printed by a program". You have machine-checked your own adjectives. A note whose thesis is that a stored verdict is a judgement taken on credit has stored eight verdicts and quoted them as measurements.'

**Correction.** Four edits, all in `/media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md`; no probe needs to change except its self-description. (1) Abstract line 36 — split the warrant instead of issuing one for everything: "Every simulation figure below is printed by a program in a public repository and re-checked by a regression suite of 115 stands. The three scores in §2 are not simulation output: the lists are the profession's, the row-by-row classification behind them is mine, and the program counts my labels rather than deriving them." (2) §2 lines 83-88 — name the agent and the kind: "Classified against the profession's own list rather than ours — the twelve ISA 315 assertions and eight accounts-payable procedures — **my own row-by-row scoring** puts **5 of 20** in need of a warrant at all. The classification is a judgement, not a run; each row is published in `db/probe_assertions.py` with the reasoning that produced it so that it can be contradicted, and **no auditor has reviewed it**." (3) §2 line 90 — same treatment: "Against documented failures **I score the instrument** 2 TOUCHES, 2 EXPOSES, 4 OUT of eight, and it *prevents none of them*. These eight verdicts are mine, argued per case in the source, not computed; by contrast the three system-error cases in the next sentence are each executed through the core (`db/probe_system_errors.py` runs `judge_sheet_claim` and `zbook.judge_book` and asserts on their return values)." (4) §2 line 77-79 — retire the borrowed authority that `probe_ledger.py:206-210` already disowns: change "eight questions an auditor asks" to "eight questions I wrote from this corpus's own vocabulary — a list one writes oneself is a list one wins, which is why the next paragraph uses somebody else's". Then add a bullet to §6 Limits: "**§2's scores are classifications, not measurements.** Where §3 reports what a program computed, §2's 5-of-20 and 2/2/4 report how I graded twenty standard items and eight documented failures. The lists are external; the grading is internal, unreviewed by any practitioner, and is published per row precisely so that a practitioner can overturn it. `probe_lattice.py:45-47` says the same of its ISA 500 grading — 'the part a real accountant would correct' — and no accountant has." Optionally tighten the harness so it stops implying more than it checks: relabel the `probe_assertions.py` / `probe_failures.py` rows in `inventory/note_claims.py:31-34` as "classification held stable (not verified)", and change `probe_assertions.py:219` from "PROBE GREEN — measured against a list we did not write" to "PROBE GREEN — classified against a list we did not write".

## 7. [BLOCKING] §2, system errors
*lens: domain*

**Quote.** Against system errors where nobody lied it does better — **2 caught of 3** — and the two are caught outright

**Defect.** One of the two catches is not a documented case. `probe_system_errors.py:96-134` (`case_cascade`) is explicitly 'The shape shared by every corrected-input scandal' — a three-line synthetic book the author wrote, whose numbers (`series_avg == -0.1`, `high_debt_penalty=2.1`, `recommended_cut == 40`) are lifted unattributed from the Reinhart–Rogoff / high-debt-threshold episode. The probe docstring nonetheless says 'Three documented cases', which is false. Worse, the real R–R failure was an Excel range-selection error inside the spreadsheet — precisely the class the note's own Whale case concedes is NOT catchable. The probe replaces that mechanism with a stipulated downstream retraction (the trivially-catchable direction) and scores it as a catch. Against real documented system errors the honest score is 1 of 2, and the 1 is a reconstruction (see the Mars finding).

**Attack.** 'Your denominator is 3 and one of your two successes is a toy you wrote to succeed. Strip it and the record against real system errors is one reconstruction and one failure. And the case you dressed as generic is Reinhart–Rogoff, whose actual failure mode you elsewhere admit you cannot catch — you kept the fame and swapped the mechanism.'

**Correction.** Two edits, both small; do not delete case 3. 1. `/media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_system_errors.py:14` — replace "Three documented cases, and each is RUN through the actual core rather than reasoned about." with something true, e.g.: "Two documented mechanisms and one constructed shape — and each is RUN through the actual core rather than reasoned about. The third is not a historical case and is not offered as one; it is the ordinary corrected-input shape, and it is here because it is the operation the ledger exists for." 2. `/media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md:94-99` — make the composition and the two senses of "caught" visible. Suggested replacement for the sentence beginning "Against system errors where nobody lied it does better": "Against system errors where nobody lied it does better, though the trio is not homogeneous and the word 'caught' does two jobs in it. Two are documented mechanisms, reconstructed and run through the core: a units mismatch is refused outright (`cannot compare 'lbf' with 'N'`, the Mars Climate Orbiter mechanism), and a spreadsheet dividing by a sum where the author meant an average returns EARNED, because the formula is well-formed and there is no second copy of the intention to compare it to. Of the two documented mechanisms, then, **one detection out of two**. The third case is constructed rather than historical — the ordinary shape in which an input is later corrected — and what it shows is not detection but propagation: once a human has found the error, the consequences travel up three storeys, including a conclusion that never named the spreadsheet. Detecting the error and moving what rests on it are different capabilities and are not summed here." 3. Optional but advisable, since it is what invited this reading: if the case-3 figures were in fact drawn from the high-debt-threshold episode, either attribute them in the docstring or change them to plainly neutral values. Unattributed but recognisable numbers on a case labelled generic is the one place the reviewer's suspicion had a foothold. No other section of the note depends on the "2 of 3" figure (grepped — §2 line 94 is the only occurrence), so the edit is local. `run_all.py:232` keys on the printed banner "SYSTEM ERRORS PROBE GREEN", which the docstring edit does not touch; the `assert verdicts.count("CAUGHT") == 2` at line 163 can stand, since the failure being fixed is a label, not a result.

## 8. [BLOCKING] §3.4 Evidence and authority are not interchangeable
*lens: internal*

**Quote.** - evidence 100% genuinely redundant, authority not: losing the commander costs **C = 0.789**; - both 100% genuinely redundant: the commander still costs **C = 1.000** — the entire collective.

**Defect.** The 0.789 is not that measurement. `python3 db/probe_criterion.py` prints, verbatim: "evidence 100% genuine, authority 100% genuine, commander lost C = 1.000" and "evidence 100% genuine, authority NOT redundant, commander lost C = 1.000". I called the function directly: C(r_evi=1.0, q=0, r_auth=0.0, q=0, target=0) = 1.0, not 0.789. The digits 0.789 come from a different row of a different table — C(r_evi=0.00, r_auth=1.00, target=7) = 0.789075, i.e. the OPPOSITE dimension redundant, and agent 7 rather than the commander. So the note's headline contrast does not exist: both configurations it names measure 1.000. `inventory/note_claims.py` has no FIGURES entry for probe_criterion.py at all, so this passes the checker green.

**Attack.** "Your one quantitative demonstration that evidence redundancy and authority redundancy are not interchangeable is a pair of numbers of which one is copied from the row for the opposite configuration at a different target. Run your own probe: it prints 1.000 twice. The contrast is manufactured, and your claims-checker does not cover this probe's figures — which is exactly the file whose numbers turned out to be wrong."

**Correction.** Three changes. 1. Replace the §3.4 bullets (note lines 173–176) with what the probe prints: - evidence 100% genuinely redundant, authority not redundant: losing the commander costs **C = 1.000**; - both dimensions 100% genuinely redundant: **C = 1.000** unchanged — full redundancy in both dimensions does not save the collective from the loss of the authority root. Then adjust the surrounding sentence, since the rhetorical "still" no longer has an escalation to sit on. The honest reading is stronger than the fabricated one for the note's own thesis: redundancy in either dimension, or both, buys literally nothing against the root of the authority tree, because the second authority parent is drawn from the same already-doomed prefix (`auth[c].append(rnd.randrange(0, c))`). A_crit is a property of concentration, not of redundancy — which is exactly what §3.4's closing paragraph already says. 2. Carry the non-interchangeability weight on the pair that is real, and label it precisely: at identical minimum redundancy 0.50, authority-rich (0.50, 1.00) loses **0.0003** and evidence-rich (1.00, 0.50) loses **0.0101** — a factor of 31, at target agent 7. This is already in the note and is correct; it should be the section's quantitative anchor rather than the bullets. If a second contrast is wanted, the extreme pair at target 7 is available but must be labelled by dimension and node, and it cuts the other way: evidence-redundant-only 0.1316 vs authority-redundant-only 0.7891. Worth deciding deliberately whether to show it, since it complicates the "authority is the binding dimension" gloss at zero redundancy in the other dimension. 3. Close the checker hole at the source, not in the checker. Add hard, configuration-bound assertions inside `db/probe_criterion.py` next to the existing `assert c_evi_only > 0.5` — e.g. `assert abs(c_root - 1.0) < 1e-9` and `assert abs(c_evi_only - 1.0) < 1e-9`, and an assert pinning the (0.00, 1.00, target 7) row near 0.7891 — so the numbers the note quotes are bound to the rows that produce them. A FIGURES entry in `inventory/note_claims.py` for probe_criterion.py is worth adding too, but do not rely on it: "0.789" matches inside "0.7891", so substring coverage alone would have passed this error. If FIGURES is extended, use full-precision, row-unique strings (e.g. "0.0101", "0.0003") and make the note print those same strings rather than "1.01%" / "0.03%", otherwise the `(not in_note) or in_code` rule silently auto-passes them.

## 9. [BLOCKING] §3.2 Redundancy contains it, with a threshold that moves
*lens: internal*

**Quote.** A statistic was corrected here. The first run reported medians ... The table is worst-case.

**Defect.** The table is not worst-case; it is the worst of 25 uniformly-sampled start nodes (`probe_topology.py:112-133`, `rs.sample(range(N), samples)` with samples=25). `db/probe_containment.py`'s own docstring names this as a method error: "'worst of 25 random failures' is not a worst case. On a hierarchy or a hub graph the adversary chooses..." And the two probes contradict each other numerically on the same model: probe_topology reports hierarchy at 90% redundancy = 10 lost; probe_containment, on a byte-identical hierarchy builder (N=100,000, E=1,000,000, branching 8, seed 20260816) at the same 90% declared redundancy, prints "THE COMMANDER -> 100,000 of 100,000 fall". Four orders of magnitude apart, both labelled worst single loss. The whole threshold column (and hence "random-local 90%, hierarchy 75%, scale-free 75%") is derived from the understated statistic; under the actual worst case the hierarchy never reaches containment at any redundancy in the sweep.

**Attack.** "You correct one statistic (median→worst) in the sentence immediately above the table, and leave a second, larger one uncorrected in the table itself — a correction your own repository already recorded in probe_containment.py's docstring. Section 3.3 measures 100,000 lost where section 3.2 reports 10, same graph, same redundancy. Which is the worst single loss?"

**Correction.** Fix in §3.2, §3.1, §3.7 and §5 of /media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md. The measurements are fine; the label and the threshold column are not. 1. Re-label the §3.2 table honestly. Replace "On three topologies at the same scale, worst single loss:" with "worst of 25 uniformly-sampled single losses" and change the last column header from "threshold" to "threshold (random loss)". Delete the sentence "The table is worst-case." — it is false as written. 2. Add the adversarial result to §3.2, from the same model. Something like: "Against a *chosen* target the hierarchy row is not 10 but 100,000. Losing the root costs the entire collective at every redundancy in the sweep, including 100%, and the reason is structural rather than a draw: every agent descends from the commander, so the second 'independent' ground is downstream of it too. Redundancy does not contain a root loss at all; containment is bounded by the worst-covered path, not the average one. The threshold column above is therefore a threshold against *random* loss. Against a chosen loss the hierarchy has no threshold." (`db/probe_containment.py` prints this; the sweep across redundancies is a two-line addition to `db/probe_topology.py` if you want it printed there too, which would be better — the number should live in the probe that owns the table.) 3. Fix the §3.1 cross-reference. "median here, and worst-case in §3.2" is now wrong. Make it "median here; §3.2 reports the worst of a random sample, and the worst *chosen* loss separately, which are three different statistics." 4. Fix §3.7. The row "redundancy threshold, hierarchy | 0.75 | 0.50–0.75" is worst-of-8 random draws (`db/probe_variance.py:96`, `samples=8`). Either raise the sample and re-label it "threshold against random loss", or add the chosen-target threshold beside it (constant: never). As it stands the variance section certifies the understated figure as seed-stable, which is true and misleading at once. 5. Add the second method error to §5. It currently records two and this is the third — and the largest. Suggested wording, matching what the repo already says: "*'The worst of 25 random failures is a worst case.'* It is not. On a hierarchy the adversary picks the commander, which a uniform draw of 25 out of 100,000 finds 0.025% of the time; the true figure is four orders of magnitude larger. Recorded in `db/probe_containment.py` and credited to the external reviewer Miteiko, and it took a second pass to propagate the correction from that probe into §3.2's table." 6. Consider adding the commander row to §3.3's table. The note reproduces part 2 of `probe_containment.py` and silently drops part 1, which is where the correction lives. Carrying the "THE COMMANDER -> 100,000" line into the note removes the appearance of having quoted selectively from a probe whose stated purpose was to repair this exact error. Nothing in §§3.4-3.6, §4, §5a or §6 is touched by this; those probes already choose their targets deliberately.

## 10. [BLOCKING] §3.1 Speed is not the problem, and the answer is useless anyway / Abstract
*lens: internal*

**Quote.** "About 52,000 of your conclusions no longer stand" is not something anyone can act on. ... Every number below is printed by a program in a public repository and re-checked by a regression suite of 115 stands.

**Defect.** 52,000 is printed by no program. `python3 db/probe_swarm.py` prints "one compromised agent, median fallout 59,716.0 of 100,000 (59.0%)". The string "52,000" occurs in exactly two places in the whole tree: the note, and the prose docstring of probe_swarm.py (line 21) — never in any output. It is a stale figure from a superseded run, quoted inside quotation marks as if it were the machine speaking, and wrong by 15% relative. `inventory/note_claims.py` has no CLAIMS or FIGURES entry for probe_swarm.py whatsoever, so §3.1's numbers are entirely unchecked. The same paragraph and the abstract also say "half the collective" for a measured 59.7%.

**Attack.** "Your abstract stakes the note's credibility on 'every number below is printed by a program … and re-checked'. The very first number in section 3, presented in quotation marks as a machine's report to a commander, is not printed by any program, is 15% off the current run, and the probe that would produce it is absent from your claims-checker. If the load-bearing verification claim fails on the first number, what is the checker's green line worth?"

**Correction.** Four changes, all needed together: 1. paper/warrant-containment-note.md:123 — replace the quoted figure with the one the machine actually prints, verbatim so the checker can bite: "About 59,716 of your conclusions no longer stand". (Writing "about 60,000" instead would leave the FIGURES entry in step 4 passing vacuously, since that test is `(not in_note) or in_code`.) 2. paper/warrant-containment-note.md:25 and :121 — "half the collective" -> "three fifths of the collective" (59.7% measured). Fix db/probe_swarm.py:118 ("Half the collective.") to match, so the source prose stops seeding the same error. 3. db/probe_swarm.py:21 — correct the docstring's stale "About 52,000" to 59,716, so the wrong figure is not re-quotable from the source. 4. inventory/note_claims.py — add the missing entries so §3.1 is actually covered: CLAIMS += ("probe_swarm.py", "one loss costs the median fallout", "one compromised agent, median fallout 59,716.0 of 100,000") FIGURES += ("probe_swarm.py", ["59,716"]) This raises the green line from 28 claims to 29 and adds one figure; re-run `python3 inventory/note_claims.py` to confirm. Optional but worth doing while there: harden the FIGURES check so a figure listed for a probe but absent from the note reports as SKIP rather than OK, otherwise deleting a number from the note silently converts a real check into a vacuous pass.

## 11. [BLOCKING] §3.2 Redundancy contains it, with a threshold that moves
*lens: methods*

**Quote.** On three topologies at the same scale, worst single loss: ... | hierarchy | 95,081 | 194 | 10 | **75%** | ... The table is worst-case.

**Defect.** It is not worst-case. `probe_topology.threshold()` (db/probe_topology.py:112-133) computes `max` over `rs.sample(range(N), 25)` — 25 uniformly random targets out of 100,000. I re-ran the identical generator, identical seed, identical redundancy dressing and identical cascade, and evaluated the low-index nodes the sample almost never reaches: hierarchy at 90% redundancy, worst-of-25-random = 10, but node 0 (the commander) = 100,000. At 95% it is still 100,000. scale-free at 90%: 20 vs 100,000. random-local at 90%: 56 vs 20,378. The corpus already knows this — probe_containment.py's own docstring says "'worst of 25 random failures' is not a worst case" and prints THE COMMANDER -> 100,000 of 100,000 fall on the same hierarchy at the same 90%. The note reproduces the superseded estimator, labels it "worst single loss", and adds a self-correction paragraph claiming the fix was made.

**Attack.** Your headline result is a sampling artefact and your own repository contains the refutation two subsections later. On the model in the §3.2 table there is no threshold at all: the true worst single loss on the hierarchy and the scale-free graph is 100% of the collective at every redundancy level you tested, including 95%. The paragraph 'A statistic was corrected here... The table is worst-case' asserts a correction that was not performed — you moved from median to max-of-25-random, which is still a random-sampling statistic, and the quantity you needed is a maximum over structurally selected targets. Everything §3.2 concludes about a threshold, its location, and its non-transferability follows from a number that is off by four orders of magnitude.

**Correction.** Do not patch the wording; the estimator and the conclusion both have to change. 1. probe_topology.py — fix the estimator. In `threshold()` (line 112-133), evaluate the max over a structurally selected target set unioned with the random draw: `targets = [0] + top-k by len(rev[n]) + rs.sample(range(N), 25)`. Report two columns, `worst-random-25` and `worst-chosen`. Delete or invert the `assert all(t is not None ...)` at line 167 — with chosen targets every topology is `never`, and the assertion as written encodes the artefact. Update the header at line 140 and the closing narrative at line 154-166, which currently asserts a threshold "in all three, in three places". 2. §3.2 — rewrite, do not relabel. Keep the existing table but retitle it "worst of 25 random compromises" and add the chosen-target column (hierarchy 100,000 / scale-free 100,000 at every level including 95%; random-local 37,786 / 20,378 / 20,356 at 75/90/95%). The honest conclusion inverts: redundancy contains a *random* loss and does not contain a *chosen* one; on these topologies there is no redundancy threshold against an adversary who picks the root. Delete "The table is worst-case." Replace the correction paragraph with what actually happened: the first run reported medians, the second moved to max-of-25-random, which is still a random-sampling statistic; the quantity needed is a maximum over structurally selected targets, and it is measured in §3.3. 3. §3.1 — fix the cross-reference. "median here, and worst-case in §3.2" is now false; either point it at the new chosen-target column or drop the contrast. 4. §3.3 — surface the row the note omits. probe_containment.py's section 1 already prints `random agent 29 / top hub 1 / top three 15 / THE COMMANDER 100,000`, and 100,000 at 100% redundancy. Put that four-row block in the note before the shared-origin table. It is the finding that makes §3.2 readable, and it is currently the only part of that probe left out. 5. §3.2/§3.3 — correct the diagnosis. The failure is not that sampling misses "the few hubs" (top hub = 1). It is that the root is not a hub at all, so neither random sampling nor degree ranking reaches it; the cascade travels because the 10% of conclusions carrying a single ground bridge every level. 6. §3.7 — the hierarchy-threshold row re-measures the same artefact with `samples=8`. Re-run it with the chosen-target estimator or remove the row; as it stands it certifies the artefact as seed-stable. 7. §5 — add a seventh failed prediction: "redundancy contains a single loss." Refuted by the repository's own probe_containment.py on the same model. This is the note's stated method (predictions refuted by its own runs, pinned by an assertion in source) applied to its headline number. 8. Abstract — "Containment is recovered by redundancy" needs the qualifier: recovered against a randomly compromised agent, not against a chosen root. 9. §6 Limits — "the existence of a redundancy threshold is probably robust" is now contradicted by the measurement and must be struck or restricted to random-target loss. 10. §7 — re-run run_all.py after (1); the 115-stand green currently depends on the assertion being removed or restated.

## 12. [BLOCKING] §1 / §3.2 / §3.3 — cascade semantics of 'two grounds'
*lens: methods*

**Quote.** A conclusion resting on two declared-independent grounds falls only if both do.

**Defect.** The code does not implement this. `redundant()` appends one extra parent and adds the node to a set `two`; `cascade()` then switches that node from `any(p in dead for p in ps)` to `all(p in dead for p in ps)` over its ENTIRE parent list (db/probe_topology.py:84-109, db/probe_swarm.py:63-80, db/probe_containment.py:65-80). At E/N = 10 the flagged nodes have a median of 11 parents. I measured the flagged sets directly: random-local 90,079 flagged, mean 11.0 parents, 0.0% with exactly two; hierarchy 90,106 flagged, mean 11.0, 0.0% with exactly two; scale-free mean 8.8, 1.4% with exactly two. So 'redundancy' is not a second ground — it converts a node from an 11-input OR into an 11-input AND. Under the semantics the note actually states (ground A = original support set, ground B = the added one, node falls iff A fails AND B fails), the same run gives random-local at 75% = 51,389 instead of the printed 20,840 (hierarchy is nearly unchanged: 195 vs 194).

**Attack.** The prose and the code describe different experiments, and the code is the optimistic one. You print 20,840 for a cell that is 51,389 under your own stated rule — a 2.5x understatement in a table you deposit. Worse, the mechanism producing your 'collapse of nearly four orders of magnitude' is that a conclusion becomes immune unless all eleven of its supports die simultaneously; that is not redundancy, it is an artefact of applying an all-of-k rule to a graph whose in-degree you set to ten. Fix the semantics or restate the claim as 'a conclusion whose supports are conjunctively pooled', which is not what any architect will read.

**Correction.** Three things, in order of necessity. 1. FIX THE SEMANTICS, then re-print. In probe_topology.py:84-109, probe_swarm.py:54-80 and probe_containment.py:49-80, record the original parent-list length k when appending the redundant ground, and evaluate flagged nodes as `any(p in dead for p in ps[:k]) and all(p in dead for p in ps[k:])` instead of `all(p in dead for p in ps)`. Re-print §3.2 (note line 134: random-local 75% becomes 51,389), §3.3 (note lines 156-159: random-target column becomes 382 / 85,238 / 97,518 / 99,992; top-three at 25% becomes 43,128) and re-run probe_variance for the §3.7 row. The three thresholds and the §3.2 "shape transfers, number does not" claim survive unchanged; §3.3's prose must change from "a quarter of nominal redundancy being fake destroys containment" to "a tenth already does" — which strengthens the warning while correcting the figures. 2. DECIDE, AND STATE, WHICH SEMANTICS IS BEING SIMULATED. probe_swarm.py:25-27 cites `earned:a|b` as the mechanism, but zbook.py:116-125 defines `a|b` as alternatives within ONE conjunctive witness slot, and under that rule the containment result disappears entirely (95,035 at r=0.90, random-local). Either (a) drop the claim that the simulation is the ledger's mechanism at scale and state plainly that the graph model gives each conclusion a composite support set plus one whole-support alternative, or (b) adopt the ledger's rule and report the negative result. Silently running a third rule that neither the note nor the ledger states is the thing that must stop. 3. FIX §3.1's PROSE (note line 120). "With one ground per conclusion" describes a graph with in-degree 1; the built graph has E/N = 10 conjunctively-required grounds per conclusion. Either build the §§3.1-3.3 graphs at in-degree 1 plus the redundant ground — which would make the original `all(...)` code correct and is the cleanest repair of all — or say "ten conjunctive grounds per conclusion" and accept the consequences for §§3.2-3.3. Note that rebuilding at in-degree 1 changes every number in §§3.1-3.3, including the "median of half the collective", so option 1 is the cheaper path.

## 13. [BLOCKING] §3.4 Evidence and authority are not interchangeable
*lens: methods*

**Quote.** Four outputs, per configuration: **C** (share lost), **r\*** (minimal real redundancy for C < 1%), **q\*** (hidden correlation tolerated at declared r = 1.0) ... Measured across ten seeds (§3.7): **r\* median 0.725, range 0.60–0.75** and **q\* median 0.35, range 0.30–0.45**

**Defect.** r* and q* are not two quantities. In `probe_criterion.build` (db/probe_criterion.py:55-58) the second ground is added iff `rnd.random() < r and rnd.random() >= q`, i.e. with probability exactly r(1-q). C therefore depends on (r,q) only through the single scalar p = r(1-q). r* is the first grid p where C < 1%; q* is defined at r = 1.0, so 1-q* is the last grid p where C >= 1%. They are the same crossing read from opposite ends. The published pair confirms it arithmetically: r* = 0.65 and q* = 0.35, i.e. 1-q* = r* exactly. The identity is a property of the generator, not a measurement.

**Attack.** You report 'the two numbers a designer actually needs' and they are one number written twice. q* = 1 - r* up to one grid step, by construction, in every seed — which is also why §3.7's two rows move together. Any reviewer who reads build() sees this in thirty seconds, and it retroactively empties the §4 sentence 'q* says containment survives about 35% hidden correlation and no more', which is just r* = 0.65 restated.

**Correction.** Fix the model, not the prose — §3.3 already contains the correct mechanism, so this is a small change with a real payoff. 1. Rewrite `build` in db/probe_criterion.py:46-68 on the pattern of `probe_containment.dress` (db/probe_containment.py:49-62): add the second ground at rate r unconditionally, record the conclusion in a `two` set, then independently mark fraction q of those pairs as `sham`; in `cascade` (probe_criterion.py:71-89) let a genuine pair die only when all its grounds are dead and a sham pair die when any is. This decouples the axes for real — a sham two-parent node is strictly more fragile than a one-parent node, so C stops being a function of r(1-q) — and it makes declared redundancy hold at r while q varies, which is what both §3.4's wording and §5a's β comparison already assume. 2. Re-measure r* and q* under the new build across the ten seeds of db/probe_variance.py:57-66 and update §3.4, the §3.7 table, the §4 sentence, and §5a's β comparison to whatever comes out. Do not carry the current 0.725 / 0.35 forward. 3. Add a pinned stand in inventory/note_claims.py asserting that q moves C at fixed declared redundancy — i.e. that C(r=1.0, q) differs from C(r=1-q, q=0) — so the collapse cannot silently return. 4. Regardless of 1-3, add one sentence to §3.4 stating the relation between declared redundancy, hidden correlation and effective redundancy in this model, and say which of the four outputs are independent. Right now that relation exists only in the probe docstring. If the re-modelling is declined, the minimum honest repair is: drop "four outputs" to three, delete "the two numbers a designer actually needs" (probe_criterion.py:149) and the "declares FULL redundancy" paragraph (probe_criterion.py:161-164), replace §3.4's "hidden correlation tolerated at declared r = 1.0" with wording that does not claim declared redundancy is held at 1.0, and state in §3.4 and §5a that q* is the redundancy threshold re-expressed rather than a second measurement — which would in turn require withdrawing §5a's β-factor comparison.

## 14. [BLOCKING] §3.5 Authority-root diversity / §3.7 seed variance
*lens: methods*

**Quote.** | A_crit, one authority root | 1.000 | constant | | A_crit, two roots | 0.117 | constant | | A_crit, three roots on a shared upstream | 1.000 | constant | ... The structural results are constant across every seed

**Defect.** `probe_roots.build(roots, quorum, shared_upstream, rnd)` (db/probe_roots.py:38-60) takes an `rnd` argument and never uses it — I checked: zero occurrences of `rnd` in the body, and `build(2,1,False,Random(1))` and `build(2,1,False,Random(999999))` return byte-identical structures. The model has no stochastic content whatsoever. probe_variance.py:75-89 dutifully re-runs it under ten seeds and prints 'constant'. Further, 0.117 is a closed-form function of BRANCH=8: it is the subtree of a first-level commander, (1+8+64+512+4096)/40000 = 4681/40000 = 0.1170. Set BRANCH=20 and it is ~0.05; BRANCH=2 and it is ~0.5.

**Attack.** Three of your six seed-stability rows are vacuous: you ran a deterministic function ten times and reported that it gave the same answer. That is not a variance study, and presenting it inside a table headed 'Ten independent seeds give' misleads the reader about what was tested. And 0.117 is not a measurement of anything — it is 4681/40000, the branching factor you chose, reported to three decimals as if it were empirical.

**Correction.** Five fixes. The first three are the finding as filed; the fourth is the false explanation in the source; the fifth is the larger error the check surfaced and should be resolved before the others are written up, since it changes the number. 1. probe_roots.py:38 — drop the unused `rnd` parameter from `build`, delete `rnd = random.Random(SEED)` at line 86, and remove the `import random` at line 29. Replace the module-level `SEED = 20260816` with an explicit comment: "no stochastic content — this model is a deterministic BRANCH-ary command tree; SEED would be decorative." Determinism should be visible in the signature rather than hidden behind an ignored argument. 2. probe_variance.py:69-90 — remove the three A_crit rows from the ten-seed loop. Re-running a deterministic function under ten seeds measures nothing. Replace with the sensitivity that actually governs the figure: sweep BRANCH over {2,4,8,16,20} and report the range, which is what a reader needs to judge 0.117. 3. Note §3.7 (lines 241-252) — delete the three A_crit rows from the ten-seed table. The heading "Every probe above runs on one seed. Ten independent seeds give:" must not stand over rows that have no seed dependence. If the rows are kept for completeness, mark them explicitly: "deterministic — the authority-root model has no stochastic content, so constancy here is not evidence of robustness." Amend line 252 ("The structural results are constant across every seed") to name which results were actually at risk under resampling and which were not. 4. probe_variance.py:100-105 — strike "which for a threshold is what one would expect — a percolation point is a property of the structure, not of the draw." That sentence explains the A_crit constancy with a mechanism that is not operating. It is true of the hierarchy threshold row and false of the three rows it is written to cover. 5. Note §3.5 (lines 199-204) and failed-prediction #2 (line 302) — resolve the target-set bug first, since it changes the reported value. Compute A_crit as a max over all agents (or a justified sample), not over the hand-picked {roots, 7, 100}; agent 1's subtree gives 0.2978. Then update the §3.5 table, restate prediction #2 with the corrected figure, and fix `assert two < 0.2` at probe_roots.py:152, which currently passes only because the worst node is not in the target set. Once corrected, disclose the model in §3.5 — 40,000 agents on a deterministic 8-ary command tree — and state plainly that the mid-level figure is the closed-form subtree share of a level-1 commander, a function of the chosen branching factor rather than a measured quantity, quoting the measured BRANCH sweep (0.205 / 0.137 / 0.117 / 0.011 at BRANCH 2 / 4 / 8 / 20). The qualitative claim the section actually needs — that a second root moves the concentration rather than dissolving it — survives all of this; the three-decimal figure presented as empirical does not.

## 15. [BLOCKING] §5a Related work (whole section) and the note as a whole
*lens: prior-art*

**Quote.** Doyle's TMS (1979) and de Kleer's ATMS (1986) already do the central operation here

**Defect.** The note contains no bibliography, no reference list, no DOIs, no volume/page data, and exactly three named authors in ~21,000 characters (Doyle, de Kleer, and 'AGM' as an initialism). Standards are named without part or annex ('IEC 61508', 'NUREG methods', 'NASA's work on ultra-reliability'). Every priority claim in §5a and every universal negative in §4 is therefore unverifiable by the reader.

**Attack.** This is being deposited with a DOI and cited in a funding proposal and a law paper. A priority claim with no citations is not a priority claim, it is an assertion. I cannot check 'so far as I can find' against a search you have not described over a literature you have not listed. Before anything else in §5a is arguable, produce a reference list — and note that the moment you do, the omissions below become visible to you too.

**Correction.** Add a References section to /media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md in the same format already used in ZTL-draft_1.4.md, and a two-sentence search note. Three concrete moves: 1. Minimum reference list (~10 items) covering everything §§2, 4, 5a name (verify each string before pasting): - Doyle, J. A truth maintenance system. Artificial Intelligence 12:3 (1979), 231–272. - de Kleer, J. An assumption-based TMS. Artificial Intelligence 28:2 (1986), 127–162. - Alchourrón, C. E., Gärdenfors, P., Makinson, D. On the logic of theory change: partial meet contraction and revision functions. Journal of Symbolic Logic 50:2 (1985), 510–530. — replaces the bare initialism "AGM". - Green, T. J., Karvounarakis, G., Tannen, V. Provenance semirings. Proc. PODS 2007, 31–40. (already ref 24 of the companion draft — reuse it.) - Dung, P. M. On the acceptability of arguments and its fundamental role in nonmonotonic reasoning, logic programming and n-person games. Artificial Intelligence 77:2 (1995), 321–357. — for "argumentation frameworks". - Cheney, J., Chiticariu, L., Tan, W.-C. Provenance in databases: why, how, and where. Foundations and Trends in Databases 1:4 (2009), 379–474. — for "lineage systems". - Fleming, K. N. A reliability model for common mode failures in redundant safety systems (1975) — supports "studied since the 1970s"; otherwise drop that date claim. - NUREG/CR-5485, Guidelines on modeling common-cause failures in probabilistic risk assessment, US NRC, 1998 — replaces "NUREG methods". 2. Fix the two standards citations to part and annex, which is where the checkable numbers live: - "IEC 61508" → IEC 61508-6:2010, Annex D (Table D.1, the scored checklist; Table D.4, the score→β mapping). While doing this, re-check "0.5% to 10%" against the actual tables: β ranges differ by subsystem (logic subsystem vs sensors/final elements), and the single span may be a conflation of two columns. If it is, the §5a sentence "an order of magnitude looser" needs re-deriving against whichever column is the fair comparator. - "the twelve ISA 315 assertions" (§2) → ISA 315 (Revised 2019), with the paragraph reference for the assertion categories. - "NASA's work on ultra-reliability" → a specific report number, or delete the phrase; as written it is a gesture, and a gesture is what the reviewer will quote. - Wirecard €1.9bn (§2) → one source line. 3. Make "so far as I can find" auditable. Add one short paragraph before the bullet list in §5a: which databases were searched (e.g. ACM DL, IEEE Xplore, Google Scholar), which query strings, over what date range, and the honest statement that the search was conducted by an LLM-assisted process rather than a systematic review. Then keep the hedge — with the search described, "so far as I can find" becomes a bounded claim instead of an unfalsifiable one, and the §5a novelty bullets become arguable rather than assertions. Do this before deposit: adding it later makes the earlier DOI version the one people cite.

## 16. [BLOCKING] §5a, novelty bullet (a); §3.4; §3.5
*lens: prior-art*

**Quote.** an **authority dimension kept separate from evidence**, so that a conclusion falls from losing permission as readily as from losing support (§3.4) — TMS justifications do not distinguish the two

**Defect.** Distributed authorization logic has separated authority from evidential support since the early 1990s and computes exactly the retraction cascade of §3.5. Abadi, Burrows, Lampson & Plotkin, 'A Calculus for Access Control in Distributed Systems' (ACM TOPLAS 15(4), 1993) gives a modal 'says' and a 'speaks-for' delegation relation; Lampson, Abadi, Burrows & Wobber (ACM TOCS 10(4), 1992) separate the authentication chain from the authorization chain. SPKI/SDSI (RFC 2693, 1999) has authorization certificates, delegation-chain reduction, and *k-of-n threshold subjects* — literally §3.5's 'quorum 2-of-3'. Blaze, Feigenbaum & Lacy, 'Decentralized Trust Management' (IEEE S&P 1996); Li, Mitchell & Winsborough's RT framework (IEEE S&P 2002), whose RT^T manifold roles encode separation-of-duty and whose credential-chain-discovery algorithm (Li, Winsborough & Mitchell, JCS 2003) is the §3.5 computation; Binder (DeTreville, IEEE S&P 2002); SecPAL; DKAL; Nexus Authorization Logic (Schneider, Walsh & Sirer, ACM TISSEC 2011); proof-carrying authorization (Appel & Felten, CCS 1999). None of these appears. Nor does the operational instance of §3.5's table: X.509 trust-anchor diversity, cross-signing, and the well-documented case of nominally distinct CAs chaining to a shared root.

**Attack.** The single central novelty claim of §3.4 is that nobody separates permission from support. Thirty years of authorization logic does precisely that, with a formal semantics, a delegation algebra, and revocation-cascade algorithms — and SPKI shipped threshold subjects in an RFC. You have rediscovered the shape of a delegation chain by Monte Carlo and reported the min-cut of a rooted tree as a finding. If §3.4 and §3.5 are the contribution, this omission is fatal to it.

**Correction.** Four concrete edits; none requires retracting a measurement. 1. §5a — add a paragraph after the TMS one, in the same conceding register §5a already uses for CCF: "**Authorization logic** is the nearest neighbour of §§3.4–3.5, and is not cited above where it should have been. Abadi, Burrows, Lampson & Plotkin (TOPLAS 15(4), 1993) give a modal `says` and a `speaks for` delegation relation; Lampson, Abadi, Burrows & Wobber (TOCS 10(4), 1992) separate the authentication chain from the authorization chain; SPKI/SDSI (RFC 2693, 1999) has authorization certificates, delegation-chain reduction and k-of-n threshold subjects; and the line continues through PolicyMaker/KeyNote (Blaze, Feigenbaum & Lacy, IEEE S&P 1996), RT (Li, Mitchell & Winsborough, IEEE S&P 2002) with credential-chain discovery (JCS 2003), Binder, SecPAL, DKAL, Nexus Authorization Logic and proof-carrying authorization. Authority as a first-class, formally specified dimension is thirty years old and this note claims nothing over it." 2. §5a bullet (a) — replace the current wording and its bare TMS warrant with the boundary that actually holds: "an **evidence dimension and an authority dimension that fail independently**, so that a conclusion falls from losing permission while support stands, and from losing support while permission stands (§3.4). Authorization logic separates authentication from authorization — identity from permission — and is otherwise authority-only: a credential is at once the evidence and the permission, so no conclusion in it falls from losing support alone. TMS justifications, symmetrically, do not distinguish permission from support. What is measured here is the containment behaviour of the two as separate requirements over one collective." 3. §5a and §3.5 — concede the quorum construct and add the operational instance, mirroring §3.3's existing "claimed here as a measurement in a warrant setting, not as a discovery". In §3.5, after the table: "The constructs are not new — SPKI/SDSI has shipped k-of-n threshold subjects since RFC 2693, and the last row is the X.509 trust-anchor problem: nominally distinct CAs, cross-signing, and certificate paths that converge on a shared root. That case is the operational corroboration of the row rather than a rival to it. What SPKI supplies is the k-of-n construct together with the assumption that the n are independent; what nothing supplies is a means of checking it, which is §4(3)." Then in §5a bullet 3, add "(SPKI's threshold subjects assume the independence this treats as the measured unknown)". 4. Before deposit, fix or annotate §3.5/§3.7 — this is what the same reviewer asks next. Either (a) make `probe_roots.build` genuinely randomised and re-run, or (b) state plainly in §3.5 that the construction is deterministic and the rows are structural arithmetic, not sampling; replace the hard-coded target list {roots, 7, 100, SUPER} in db/probe_roots.py:89-92 with a real maximum over all agents (agent 1 alone gives 0.298 against the reported 0.117, so the "worst single loss" label is currently false); and drop the three A_crit rows from §3.7's ten-seed table, or mark them "deterministic — not a seed result", since db/probe_variance.py:75-89 re-measures an identical graph ten times.

## 17. [BLOCKING] §4, the ladder; §5a novelty bullets
*lens: prior-art*

**Quote.** Attestation supplies (1). Provenance systems supply (2). Nothing found supplies (3), (4) or (5)

**Defect.** Rung (5), 'currentness', is a named and studied primitive. Burrows, Abadi & Needham's 'A Logic of Authentication' (ACM TOCS 8(1), 1990; DEC SRC RR 39, 1989) contains *both* the jurisdiction postulate ('P controls X' — authority, distinct from evidential belief) and the freshness/nonce-verification postulate (currentness), i.e. rungs (1)/(authority) and (5) formalised in 1989. Stubblebine, 'Recent-Secure Authentication: Enforcing Revocation in Distributed Systems' (IEEE S&P 1995) is a whole paper on bounding staleness of a credential as an explicit recency requirement. Add certificate-revocation freshness (Rivest, 'Can We Eliminate Certificate Revocation Lists?', FC 1998; OCSP validity windows; short-lived certificates), and 'dynamic safety cases for through-life safety assurance' (Denney, Pai & Habli, ICSE 2015). Rung (2) is also asserted without naming what supplies it: W3C PROV-DM/PROV-O (W3C Recommendation, 2013) and the Open Provenance Model (Moreau et al., FGCS 2011) are the standards and are absent.

**Attack.** 'Nothing found supplies (5)' is a universal negative, stated without a hedge, about a primitive that has had a name in security since 1995 and a logical postulate since 1989. If the search missed BAN logic — the single most cited paper on reasoning about the freshness and authority of credentials — the reader is entitled to conclude the search was not a search.

**Correction.** Three edits. No measurement changes; this is citation and scoping. 1. /media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md:274-278 — replace "Attestation supplies (1). Provenance systems supply (2). Nothing found supplies (3), (4) or (5)" with, in substance: "Attestation supplies (1). Provenance systems supply (2) — W3C PROV-DM/PROV-O (Recommendation, 2013) and the Open Provenance Model (Moreau et al., FGCS 2011) are the standards, provenance semirings and database lineage the formal side. Rung (5) has a literature this note missed in draft, and it is not a small one: freshness is a postulate in BAN logic (Burrows, Abadi & Needham, DEC SRC RR 39, 1989; ACM TOCS 8(1), 1990), bounding the staleness of a credential is the whole subject of recent-secure authentication (Stubblebine, IEEE S&P 1995), and revocation freshness — CRLs, OCSP validity windows, short-lived certificates (Rivest, FC 1998) — is deployed infrastructure. What those supply is the currentness of a CREDENTIAL: whether this key, certificate or message is still valid now. What (5) asks is the currentness of a STRUCTURAL judgement — whether two grounds are still independent, whether the map is still complete enough for this decision — and there no revocation infrastructure attaches, because per §3.3 nothing establishes the judgement to begin with. Dynamic safety cases (Denney, Pai & Habli, ICSE 2015) raise exactly this for through-life assurance arguments, and propose it rather than supply it. So, precisely: (1) and (2) are supplied; (5) is supplied for credentials and not for structural judgements; (3) and (4) I have found nothing supplying at all." 2. Same file, :363-364 — the authority bullet. Append to it: "TMS justifications do not distinguish the two; BAN logic's jurisdiction rule (`P controls X`, distinct from `P believes X`) does separate authority from evidential belief, in 1989, for authentication statements. What is not there is that separation carried through a dependency cascade as a measured containment quantity (§3.4)." 3. /media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_currentness.py:166 — "a primitive nobody owns" is the unnarrowed claim in executable form and must move with the note. Replace with e.g. "a primitive that is owned for credentials and unowned for structural judgements," and add the BAN/Stubblebine/OCSP/dynamic-safety-case pointers in the module docstring so the source carries the same scoping the paper does. Optionally consider whether §5a should carry a one-line admission in the same register as the CCF paragraph — this is the second nearest-neighbour miss, and the section's credibility rests on volunteering them.

## 18. [MAJOR] §3.6 An incomplete map, and a stale one
*lens: defense*

**Quote.** a collective acting without a gate spends **about two thirds of its actions outside its own containment requirement** — 66% even at zero loss, because delay alone suffices

**Defect.** The 66% is an artefact of the simulation horizon, not a property of ungated action. In `probe_gate.mission`, drift is monotone and irreversible (`true[i] = (a, a)` is never undone), so the true correlated share crosses the 2% limit at roughly step 102 of 300 and never returns. The reported share is therefore 1 − 102/300. I varied STEPS with everything else fixed: 100 steps → 0.0%; 300 → 66.0%; 1,000 → 89.8%; 3,000 → 96.6%. The number is chosen by the horizon. Separately, `rate` is a pure multiplier on both `acted` and `violations`, so the violation share is exactly invariant to action rate — I measured 66.0000% at rate 1, 10, 100 and 1000. "Two thirds of its actions" is therefore "two thirds of the timeline"; action rate is decoupled from the world. This also makes the probe's own §3 table, labelled "THE NUMBER A PROGRAMME OFFICE ASKS FOR — maximum action rate," degenerate: it can only ever return the top of the hardcoded list (100) or 0, which is what it prints.

**Attack.** "Your headline dynamics number is a function of how long you chose to run the loop, and your model literally cannot represent the variable — action rate — whose table you addressed to my office by name. Actions in your simulation consume no time, alter no dependencies and delay no updates. Calling that 'the number a programme office asks for' tells me you have not thought about what a programme office does."

**Correction.** Four changes, in `/media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md` §3.6 (lines 228-230) and `/media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_gate.py`. **(a) Report the onset, not the share.** The physical quantity in this model is when correlated drift crosses the limit, not what fraction of an arbitrary window sits after it. Replace the sentence with something like: "Under this model's drift, the collective's true correlation crosses its own 2% limit at step 102 and, because drift here is absorbing, never returns; nothing in the system reports the crossing at the time. The *share* of a mission spent in violation is therefore 1 − 102/T and is set by the mission length, not by the channel — 0% at T = 100, 66% at T = 300, 90% at T = 1000." That keeps the finding (an unbounded warrant goes silently bad and stays bad) and drops the number that is really a choice of T. **(b) Delete "because delay alone suffices."** It is false in the model: at zero loss, mean_delay 1 through 1000 all give exactly 66.0%, because `seen`/`c_pred` are computed and unused when `gated=False`. If the note wants delay to matter to Table 1, the ungated arm has to act on `seen` rather than ignore it. Also add a line to `probe_gate.py` noting that Table 1's row-to-row variation is RNG-stream noise from `rnd.expovariate` being skipped on lost updates (`probe_gate.py:71-73`) — not an effect of loss — or, better, decouple the streams (`random.Random(seed)` for drift, a second `Random` for channel events) so the rows are genuinely comparable and Table 1 collapses honestly to one row. **(c) Say "timeline", not "actions", or give action rate a mechanism.** As written, `rate` is a pure multiplier on both accumulators and cancels in the share. Either restate as "two thirds of the mission timeline" and add to §6: "Actions in this model consume no time, alter no dependencies and delay no updates; action rate is a multiplier on the accumulators and the violation share is exactly invariant to it (measured 66.0000% at rates 1, 10, 100 and 1000)." Or make rate consequential — e.g. drift per step scaling with rate, or actions and updates competing for the same channel bandwidth — which is what would make the programme-office framing earn its name. **(d) Fix or delete Table 3 (`probe_gate.py:131-141`).** Under (c)-as-written it can only print the top of the tuple on line 137 or 0. Either delete it and the "THE NUMBER A PROGRAMME OFFICE ASKS FOR" label, or implement the rate feedback in (c) so the maximum is interior. As a minimum, the assertion at line 202 should be joined by one pinning the degeneracy so it cannot be reintroduced silently, e.g. `assert mission(20, 1, l, d, gated=True)["violations"] == 0) == (mission(20, 100, l, d, gated=True)["violations"] == 0)`. **(e) Add to §6 Limits:** "The §3.6 dynamics run on an absorbing drift process: correlation only ever increases. No ungated share reported there is stationary — every one tends to 100% as the mission lengthens, and each is quoted for a 300-step horizon."

## 19. [MAJOR] §3.3 Declared redundancy resting on a shared origin
*lens: defense*

**Quote.** **The collective's own report never moves** — 90% at every row

**Defect.** Tautological, and presented as the finding of the section. In `probe_containment.dress`, 0.90 is the input parameter `frac_two`; the "report" is not computed by anything — it is the number the experimenter typed in. Nothing in the system could make it move. The genuine content of §3.3 is that the cascade grows with hidden sharing while a *declared* parameter is held fixed by construction. Stating that as a discovered invariance ("never moves") dresses an experimental control as a result. The same section then generalises to "neither would an attestation layer," which is an assertion about a class of systems supported by no experiment and no citation to any attestation system.

**Attack.** "You held a parameter constant and reported that it stayed constant. That is the definition of a control, not a finding. And the sentence you attach to it — that no attestation layer could tell the rows apart — is a claim about in-toto, SLSA and remote attestation that you support with neither a run nor a reference."

**Correction.** Two edits, both in §3.3 of /media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md. 1. note:150-152 — take the bold off the control and put it on the result. Replace: "Hold redundancy at 90% and raise the share of "independent" pairs that secretly share an origin. **The collective's own report never moves** — 90% at every row:" with something like: "Hold declared redundancy fixed at 90% — by construction the same conclusions declare two grounds at every row — and raise the share of those pairs that secretly share an origin. The declared figure is held, not discovered; the point is that it is the only figure the collective publishes, and it is insensitive to the variable below:" Then bold the sentence after the table instead: "**A quarter of nominal redundancy being fake destroys containment while the system reports 90%.**" 2. note:164-166 — scope and cite the attestation claim. Replace "and neither would an attestation layer" with an identity-scoped version plus the named systems, e.g.: "…and neither would an identity-attestation layer: both grounds are genuine documents from genuine agents that happen to rest on the same thing. Attestation frameworks that do carry descent — in-toto link/attestation metadata, SLSA provenance, which record the materials of each step — could in principle expose a shared material, but only one recorded within the attested scope; a shared model, sensor feed or authority root outside it stays invisible (§6, 'citations are honoured, never discovered')." Hoist the reasoning already written in /media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_containment.py:12-17 (identity/origin vs provenance) into the note rather than leaving it only in the source, and add in-toto/SLSA/remote attestation to the §5a related-work list at note:346-357 alongside provenance semirings and lineage systems. No change needed to the probe or to the numbers — the run reproduces the table exactly (0%: 29/1/15; 10%: 4,102/18/49; 25%: 29,391/3,793/20,099; 100%: 99,992/94,180/94,180).

## 20. [MAJOR] §2 What an ordinary store cannot say
*lens: defense*

**Quote.** Scored against the profession's own list rather than ours — the twelve ISA 315 assertions and eight accounts-payable procedures — the score falls to **5 of 20** needing a warrant at all.

**Defect.** The *list* is the profession's; the *scoring* is entirely the author's. `probe_assertions.py` is a hardcoded table of 20 rows each carrying a hand-typed label PLAIN / WARRANT / NEITHER; the "5 of 20" is a `Counter` over the author's own labels. Identically, `probe_failures.py`'s "2 TOUCHES, 2 EXPOSES, 4 OUT of eight" is eight hand-written verdict strings counted by a `Counter`, and "2 caught of 3" in probe_system_errors is the same pattern. Single unblinded rater, no second coder, no inter-rater agreement, no pre-registered rubric, no auditor consulted (the probe's docstring states the curator declined to approach one). The abstract's "Every number below is printed by a program" is true and epistemically empty here — the program prints back strings the author typed — and `note_claims.py` "verifies" them by grepping the output for those same strings.

**Attack.** "Your framing implies external scoring. It is self-assessment with a Counter in front of it, by the party with the interest, unblinded, n=1 rater. Change three labels and the number changes; nothing in your suite would notice. I would not accept this from a graduate student, and it sits in §2 where I am still deciding whether to read §3."

**Correction.** Two edits, both additive; no number changes. 1. §2, replace lines 83-85 ("Scored against the profession's own list rather than ours — the twelve ISA 315 assertions and eight accounts-payable procedures — the score falls to **5 of 20** needing a warrant at all.") with: "The list is the profession's; the mapping is mine. Taking the twelve ISA 315 assertions and eight accounts-payable procedures verbatim and marking each PLAIN / WARRANT / NEITHER by hand — one unblinded rater, no second coder — the score falls to **5 of 20** needing a warrant at all. The item set is external; the verdicts are not. A reader who disagrees with four rows gets a different number, and every row is printed with its reasoning in `db/probe_assertions.py` so that disagreeing is cheap. The same applies to the eight-case failure table below." 2. §6 Limits, add a bullet (and it belongs before the Lean one, since it is about the weakest evidence in the note, not the strongest): "**Two tables in §2 are my own coding, not a measurement.** The ISA-315/payables scoring (5 of 20) and the documented-failure verdicts (2 TOUCHES, 2 EXPOSES, 4 OUT) are hand-assigned labels that a program counts. One unblinded rater, no second coder, no inter-rater agreement, no auditor consulted — deliberately, on the ground that a friendly professional can nod but cannot refute, which is a reason not to run a weak test and not a reason to trust a self-coded one. The regression suite pins these counts against silent drift; it does not validate them. §2's third figure is different in kind: the system-error result (2 caught of 3) executes each case through the calculus, and the verdict follows from the computed disposition rather than from a label." 3. Optional but consistent — abstract line 36, "Every number below is printed by a program in a public repository and re-checked by a regression suite of 115 stands." → append: "The two hand-coded tables of §2 are the exception the phrase does not cover, and §6 says which they are." Cheapest genuine strengthening if the curator wants the number to survive contact: publish the 20 rows as a table in an appendix (they already carry their reasons in the source), so the reader can re-score and see where they differ. That converts an unverifiable count into a falsifiable one without needing a second coder.

## 21. [MAJOR] §3.7 Are these figures, or single observations?
*lens: defense*

**Quote.** Every probe above runs on one seed. Ten independent seeds give:

**Defect.** The framing implies the section closes the variance question for §3; it covers four quantities out of roughly ten headline figures. `probe_variance.py` measures r*, q*, three A_crit rows and the hierarchy threshold — and nothing else. It does NOT touch: §3.3's entire shared-origin table (which is the abstract's headline, "a quarter of it being fake"); §3.1's median-half; the random-local and scale-free thresholds; §3.6's blindspot table; §3.6's 66% and the gate's 270/170 leaks. Worse, the one threshold row it does check is measured at `samples=8` where probe_topology uses `samples=25` — worst-of-8 versus worst-of-25 random targets is a different statistic, so the harness checks a weaker quantity than the figure it certifies. The file's own comment warns against exactly this error ("A variance harness measuring a different quantity from the figure it is checking is worse than no harness") and then commits it. No confidence intervals anywhere; medians and min–max of n=10 only.

**Attack.** "Your variance section is titled as if it covers the note and covers 40% of it, omitting the result you put in the abstract. And your worst-case-of-8 does not certify a worst-case-of-25 — you wrote the warning about that yourself, two lines above the call that violates it. Ten runs of one generator with no CIs is not a variance analysis; it is a spot check advertised as one."

**Correction.** Three edits, in order of severity. 1. FIX THE STATISTIC (required — a published number is wrong). In /media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_variance.py:96, drop `samples=8` so the call uses probe_topology's own default of 25: `lambda m: m.threshold(m.hierarchy)[1]` Cost measured: about 100 s for ten seeds. Then correct note line 250 to `| redundancy threshold, hierarchy | 0.75 | constant |` and move it into the "structural results are constant" group in the paragraph at note:252-254 (the 0.50-0.75 spread does not exist). 2. STATE THE COVERAGE (required). §3.7 must name what it did not measure. After the table, add one sentence in the note's own register, e.g.: "Six quantities were re-measured. The §3.3 shared-origin table, §3.1's median, the random-local and scale-free thresholds, and §3.6's blindspot, delay and gate figures remain single-seed, and this section does not speak for them." Also restrict note:256 from "This answers whether the figures are artefacts" to "these figures". 3. CLOSE THE GAP ON THE ABSTRACT'S HEADLINE (strongly advised). The one omission that is hard to defend is §3.3 — it is the result the abstract leads with. probe_containment.py builds its RNG at call time (lines 93, 104, 119, 133), so `with_seed("probe_containment", s, ...)` works as-is; add the 25%-shared row across the ten seeds. Same for probe_blindspot.py:112. WARNING for whoever extends the harness: probe_swarm.collective (probe_swarm.py:44), probe_gate.mission (probe_gate.py:53) and probe_currentness.run (probe_currentness.py:51) take `seed=SEED` as a DEFAULT ARGUMENT, bound at import time — patching `m.SEED` will silently do nothing there and ten "independent" seeds will return ten identical numbers. Pass the seed explicitly for those three, or the harness will manufacture a false "constant".

## 22. [MAJOR] §2, Mars Climate Orbiter
*lens: domain*

**Quote.** a units mismatch (`cannot compare 'lbf' with 'N'`, the Mars Climate Orbiter mechanism)

**Defect.** The 'catch' does not survive contact with the case. `_unify_units` (`znum.py:249-260`) is string inequality: no dimensional analysis, no conversion. Two consequences I verified by running the judge. (a) Dimensionless unifies with anything, so if the source quantity carries no unit — which is exactly the MCO condition, the SM_FORCES file being numeric data with the unit stated only in an interface specification — the judge returns EARNED: `parse_quantities('a=1 earned:small-forces-file, b=1 earned:nav-spec N')` → disposition EARNED. The instrument does not catch MCO as it occurred; it catches a reconstruction in which the missing declaration has been supplied by hand. `probe_system_errors.py:55-59` states this condition ('the unit must travel WITH THE VALUE'); §2 drops it and says 'caught outright'. (b) It also returns E for `'N'` vs `'newton'` — a false refusal on identical units, which shows the check is symbol mismatch, not units. Additionally the quantities are coded as `lbf`/`N` (force) while the actual and docstring mismatch is lbf·s / N·s (impulse), and lbf/N are commensurable, so dimensional analysis alone would not have caught MCO — only unit-with-scale awareness would.

**Attack.** 'Your headline catch is a string comparison, it fires on "N" vs "newton", and it silently passes the exact configuration that killed the orbiter — a number with no unit on it. Meanwhile unit-typed arithmetic is thirty years old (Ada, F# units of measure, Boost.Units, Pint, Frink) and MCO is the standard motivating example in every one of those. §5a does careful related work for §1 and §3.3 and none at all for the one §2 result you claim as a win.'

**Correction.** Four changes; the first two are required, the third is required for §5a's own standard, the fourth is a code/text hygiene fix. 1. §2, line 94-96 — state the condition inline instead of "caught outright". Replace: "Against system errors where nobody lied it does better — **2 caught of 3** — and the two are caught outright: a units mismatch (`cannot compare 'lbf·s' with 'N·s'`, the Mars Climate Orbiter mechanism) and a corrected input whose consequences travel up three storeys." with: "Against system errors where nobody lied it does better — **2 caught of 3** — but each catch carries a condition, and the Mars one is not met by the case it is named for. The comparison refuses rather than converts (`cannot compare 'lbf·s' with 'N·s'`) *only if the unit travels with the value*. In the orbiter the SM_FORCES file was numbers, with the unit stated in an interface specification; run that configuration — one side carrying no unit — and the instrument returns EARNED, 'grounded outright', with nothing marking it. What is caught is the mechanism; the case as it occurred is caught only after the missing declaration is supplied by hand." 2. §6 Limits — add the twin of "Citations are honoured, never discovered": "**Units are honoured, never inferred.** A quantity with no unit unifies with any unit, so a partly annotated comparison passes silently and positively — the same unmarked confidence §2 charges plain SQL with. The check is also symbol identity, not dimension and not scale: `N` and `newton` do not unify, and no conversion is ever applied." 3. §5a — add a paragraph beside the CCF and TMS ones: "**Units-of-measure typing** is the nearest neighbour of the §2 units result, and the Mars Climate Orbiter is its standard motivating example, so the result claims nothing there. Ada's physical types, Kennedy's dimension types and their descendant F# units of measure, Boost.Units, Pint and Frink all carry units in the type or the value and check them at compile or evaluation time, with scale and conversion. What §2 exercises is weaker than any of them: symbol identity at the additive/comparative boundary, no dimensions, no scale, and a dimensionless wildcard. It is reported as the floor refusing a comparison, not as a contribution to that line." 4. Code and pinned strings — make the probe match its own docstring: in db/probe_system_errors.py:44-45 use `lbf·s` and `N·s`, and update the pinned substring in run_all.py:233 and inventory/note_claims.py:38 to `cannot compare 'lbf·s' with 'N·s'` (verified: that comparison also returns E). While there, add a second run inside `case_mars` that judges the unit-less source against the N-declared spec, prints the EARNED it returns, and asserts it — so the condition is pinned by an assertion the way §5's failed predictions are, rather than only by prose. Optional, if the catch is to be made real rather than only correctly described: in `_unify_units` (znum.py:249-260) take a flag distinguishing multiplicative combination from additive/comparative context, keep the dimensionless wildcard for the former, and in the latter either raise `_NoReadings` or return the quantity ON CREDIT when one side is unit-less and the other named. Do not add conversion — the refusal-not-conversion stance is defensible and should stay — but a synonym table that turns `newton` into `N` at parse time (or refuses with a named suggestion) removes the `N`/`newton` embarrassment without weakening it.

## 23. [MAJOR] §2, ISA 315 labels
*lens: domain*

**Quote.** the score falls to **5 of 20** needing a warrant at all. Most of an auditor's day is ordinary querying.

**Defect.** Two problems. (i) The labels behind 5-of-20 are wrong on at least three assertions, each in the same direction. Cut-off is marked PLAIN, 'a date comparison' (`probe_assertions.py:46-48`) — but cut-off is precisely whether the *recorded* date matches the actual transfer of control, answerable only against shipping documents / goods-received notes, i.e. the warrant. Accuracy is PLAIN, 'arithmetic against a source document' — the source document is external evidence not in the database; the join is to a scan at best. Classification is PLAIN, 'a foreign key to a chart of accounts' — the foreign key records the judgement (lease vs finance, capitalise vs expense) without answering whether it is *proper*, which is the assertion. Each label begs the question by assuming the evidence is already in the table. (ii) 'Most of an auditor's day is ordinary querying' is inferred from an unweighted count of 20 list items. There is no time data anywhere in the corpus, the items are not equally costly (completeness and valuation consume disproportionate effort precisely because they are hard), and most audit hours go to planning, risk assessment, walkthroughs, judgement on estimates and review — none of which is querying at all.

**Attack.** 'You have inferred how a profession spends its day from the length of a bullet list. And your headline humility number is unreliable in both directions, because three of your six PLAIN labels are PLAIN only if you assume the external document is already a row in your table — which is the assumption your whole note exists to attack.'

**Correction.** Three edits, all cheap, and the note's own §5 register is the natural home for them. 1. `db/probe_assertions.py`: move Cut-off (L45-48) and Accuracy (L43-44) to "WARRANT", with rationales that name the ground — Cut-off: "whether the recorded date matches the transfer of control, answerable only against a despatch note or GRN — a ground, not a date"; Accuracy: "the source document is external; the warrant is what records that this amount rests on inv-17, which is precisely §1's example". Move Classification (transactions, L49-51) to "WARRANT" too — "the foreign key records the judgement, it does not answer whether the judgement is proper" — and apply the same treatment to Classification (balances, L68) or state explicitly why the balances case differs. Update the printed counts and the summary at L150-156; the assert at L218 checks `c["NEITHER"] == 2`, which survives, but re-check it if anything moves to NEITHER. 2. Note §2 L83-85: replace with the corrected figure and drop the time claim. E.g. "Scored against the profession's own list — the twelve ISA 315 assertions and eight accounts-payable procedures, though the PLAIN/WARRANT/NEITHER verdicts are ours, not theirs — 8 of 20 need a warrant at all. That is a count of items, not of hours: the items are not equally costly, and the two the profession ranks hardest are one-twentieth each here." 3. Add a seventh entry to §5: "'Cut-off, Accuracy and Classification are ordinary querying.' Wrong, and wrong in one direction: each was PLAIN only on the assumption that the external document is already a row in the table — the assumption this note exists to attack. The first score of 5 of 20 was too modest for a bad reason." Add one line to §6 Limits: the assertion verdicts are the author's judgement over somebody else's list, and are contestable per-assertion.

## 24. [MAJOR] §3.6 An incomplete map, and a stale one
*lens: internal*

**Quote.** Under delay and loss, a collective acting without a gate spends **about two thirds of its actions outside its own containment requirement** — 66% even at zero loss, because delay alone suffices

**Defect.** "because delay alone suffices" is refuted by the code it cites. In `db/probe_gate.py:53-98`, the ungated arm (`gated=False`) never reads `c_pred`, `staleness`, `obs_rate` or `margin` — the observation channel is not consulted at all. I ran `mission(20, 10, loss=0.0, mean_delay=d, gated=False)` for d = 0, 3, 50, 1000: all four return violations=1980 of 3000, share 66.0%, identical. The 66% is 1 − t_cross/STEPS, set entirely by drift rate (20 agents/step), N, SOURCES, LIMIT=2% and STEPS=300 — the true alias share crosses 2% around step 100 of 300. The variation across the note's five channel rows (1980/2070/2030/2030/2010) is RNG-stream jitter, not an effect of loss: rows 3 and 4 differ only in mean delay (3 vs 10) and print the identical 2030.

**Attack.** "Set mean_delay to 1000 in your own probe: still 66%. Set it to 0: still 66%. The ungated arm cannot see the channel — delay is a dead parameter in it. Your 66% is a restatement of where you put the drift rate relative to the limit, dressed as a finding about acting on unbounded warrant. And your own table proves it: 30% loss at delay 3 and delay 10 give the same 2030 violations."

**Correction.** Two changes, one required and one that recovers the intended claim. (1) REQUIRED — strike the false cause and label the arm. Replace the §3.6 sentence (paper/warrant-containment-note.md:228-230): "Under delay and loss, a collective acting without a gate spends **about two thirds of its actions outside its own containment requirement** — 66% even at zero loss, because delay alone suffices — and nothing reports it at the time." with: "A collective acting without a gate never consults the channel at all: it acts unconditionally, so its violation share is fixed by how long the world is out of limit — here 66%, the 198 of 300 steps after the true alias share crosses 2% at step ~102. This is a baseline, not a channel effect; delay and loss do not move it (identical at mean delay 0 and 1000), and the small variation across channels in the probe's first table is RNG-stream jitter." Mirror the same edit in db/probe_gate.py:150-155 — delete "because delay alone is enough" and "on ANY of these channels", keep "This is not a lossy-channel problem", and add that it is not a delay problem either. (2) RECOVERS THE CLAIM — add the arm that actually measures staleness. The intended finding ("acting on a stale map violates containment while the map reports safety") needs a map-trusting arm: act while `c_pred <= LIMIT` with no margin, i.e. the naive collective that believes its own observed graph. That arm does respond to the channel. Measured on the same seed/model (my instrumentation, not yet in the repo): loss 0.0, delay 0 -> acted 1030, viol 10, 1.0% loss 0.0, delay 3 -> acted 1050, viol 30, 2.9% loss 0.0, delay 10 -> acted 1120, viol 100, 8.9% loss 0.0, delay 50 -> acted 1480, viol 460, 31.1% loss 0.3, delay 3 -> acted 1510, viol 540, 35.8% loss 0.3, delay 10 -> acted 1580, viol 610, 38.6% Monotone in delay at fixed loss, and 1.0% at zero delay. That is the table that supports "delay alone suffices" — and it makes the gate's value legible as the gap between the naive arm and the gated arm, rather than against a control that was never looking. Add it as a third column in probe_gate table 1 (`gated=False` / `naive` / `gated=True`), and pin the monotonicity with an assert so it cannot be tuned away. Also add to §5 (Predictions that failed): the ungated control was read as evidence that delay causes the violations; it is channel-blind by construction, and the figure restates the model's drift schedule. That is the same class of error as §5.3 and belongs beside it.

## 25. [MAJOR] §5 Predictions that failed, item 6 / §4
*lens: internal*

**Quote.** 6. *"An autonomous collective may act on a warrant only while…"* A normative claim this work does not establish. Corrected to *continue to rely*.

**Defect.** Direct contradiction with §4 of the same note, which says of the final wording: "Not *may act*, and not *may rely* either: both are permissions, and nothing here establishes one." `db/probe_gate.py` records three passes: "may ACT" (rejected), "may continue to RELY" ("which is narrower and still a permission", also rejected), and the final "may continue to TREAT". §5 reports the correction as landing on the second, already-rejected formulation. Also, the §5 preamble claims "Each is pinned by an assertion in the source so it cannot be quietly tuned away" — probe_gate.py's only assertion is `assert leak_free < 5 and leaked > 0`, which pins item 5 (the leak), not item 6. Nothing in the source pins the wording.

**Attack.** "Your section 4 says 'rely' is a permission you cannot establish; your section 5 says you corrected the sentence to 'rely'. In a note that will be cited in a law-informatics paper for precisely this act/rely/treat distinction, the two sections give different final wordings — and the claim that every failed prediction is pinned by an assertion is false for this one."

**Correction.** Rewrite §5 item 6 (paper/warrant-containment-note.md:309-310) to record the three-pass chain and land on "treat": "6. *\"An autonomous collective may act on a warrant only while…\"* A normative claim this work does not establish. The first correction, *may continue to rely*, was refused for the same reason — narrower, still a permission. The wording that stands is §4's *may continue to treat*, printed by `db/probe_gate.py` and pinned as a required marker in `run_all.py`." Then loosen the §5 preamble (lines 297-298) so it is true of this item too: "Each is pinned in the source — by an assertion where it is a number, by a marker `run_all.py` demands back from the output where it is a wording — so none can be quietly tuned away." Finally fix the stale label at inventory/note_claims.py:70: change "reliance, not permission" to "treatment, not permission" so the label matches the marker it pins.

## 26. [MAJOR] §5 Predictions that failed, item 4
*lens: internal*

**Quote.** 4. *"Hiding 5% of dependency edges takes a predicted 1% to an actual 40%."* The direction held; the magnitude did not — 28% relative.

**Defect.** The 28% is not the 5% row. `python3 db/probe_blindspot.py` gives at 5% hidden: predicted 0.086, actual 0.089, error +0.004 — a 3.4% relative underestimate. The 28% is computed in the probe as `rel = (full[2]-full[1])/full[2]` where `full = rows[-1]`, i.e. the 100%-hidden row (0.064 vs 0.089), and the probe's prose says so explicitly: "Here the worst is 0.064 predicted against 0.089 actual". Pairing a 5% antecedent with a magnitude measured at 100% hiding overstates the refuted magnitude by a factor of eight.

**Attack.** "You refute your own prediction with a number measured twenty times further along the x-axis than the prediction's condition. At the 5% you actually predicted about, the answer is 3.4%, not 28% — which makes your prediction wrong by more than you admit, not less."

**Correction.** Label the condition in both places. §5 item 4 (paper/warrant-containment-note.md:306-307) becomes: 4. *"Hiding 5% of dependency edges takes a predicted 1% to an actual 40%."* The direction held; the magnitude did not. At 5% hidden the underestimate is 4% relative (0.086 predicted against 0.089 actual); 28% is this model's ceiling, reached only at 50% hiding and above, where C_predicted bottoms out on the authority side. §3.6 (:223) becomes: "The magnitude is modest — 4% relative at 5% hidden, rising to a 28% plateau at 50% and above — and the reason matters more than the number: ..." Optionally add the 100% row to the §3.6 table (identical to the 50% row) so the plateau is visible, and note in the probe prose (probe_blindspot.py:132) that `full` is the 100% row and that the value saturates from 50% on.

## 27. [MAJOR] §3.5 Authority-root diversity — the generator
*lens: methods*

**Quote.** | 2 roots, either suffices | 0.117 | ... It stops at two, and only if the two are really two.

**Defect.** In `probe_roots.build`, for every agent c >= BRANCH the per-root chain node is `auth[(c,k)] = [c // BRANCH]` — the same plain integer parent for every k (db/probe_roots.py:54-56). The k 'separate command trees' therefore converge onto a single node one level below the top: only agents 0..7 actually hang off distinct roots. Below the first level every agent has, in effect, one authority parent regardless of how many roots you declare. Separately, `a_crit()` maximises over a hand-picked candidate list — the roots, node 7, node 100, and the super-root — 4 to 6 nodes out of 40,000.

**Attack.** Your claim is that root diversity is real diversity only if it is end-to-end, and your generator violates that for 99.98% of the agents: two roots is two roots for eight agents and one root for the other 39,992. The 0.117 you attribute to 'the concentration moving to a mid-level agent' is simply the subtree under the single convergent parent your builder created. And 'A_crit = worst single loss' computed over a four-element list you chose after knowing the structure is not a worst case; it is a spot check that happens to contain a maximiser.

**Correction.** Two fixes, both in /media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_roots.py; the second is load-bearing. A. Make a_crit() a worst case rather than a spot check. Replace the hand-picked `targets` with an exhaustive sweep over range(N) + root_ids (+ SUPER when shared) — 40,000 cascades ran in seconds here, no approximation needed. If cheap exhaustive sweeps are wanted at larger N, compute subtree sizes once instead of re-cascading. B. Fix build() so the k trees are separate below the top, as its own comment at lines 52-53 promises: give each tree its own hierarchy over the agents (e.g. a per-tree permutation, parent = perms[k][i//BRANCH], with the first BRANCH positions hanging off root k), so an agent's k authorisations have k different parents. Then quorum actually means something below level 1. Then restate §3.5 with the numbers that fall out. With A alone, A_crit(2 roots) = A_crit(3 roots, 2-of-3) = 0.298, and `assert one > 0.9 and two < 0.2 and sham > 0.9` must be re-pinned (two < 0.35). With A+B, A_crit = 0.0001 for both, and three downstream passages must change: the §3.5 table rows and the sentence "the worst single loss is no longer a root but a mid-level agent carrying a subtree… Root diversity buys the mission; it does not buy the branch"; the §3.7 row "A_crit, two roots 0.117 constant"; and §5 item 2, which must move out of "predictions that failed" — under an end-to-end-independent generator a second root does make the worst loss negligible, which is what was originally predicted. If instead the intended model really is one command hierarchy with several top authorities, that is defensible but must be said in the text and the row relabelled: it is not "2 roots, either suffices" in the independent sense the section contrasts against the shared-upstream row, since below the first level it is precisely the non-end-to-end independence the note condemns. Either way, add to §6 Limits (or the probe docstring) that A_crit is a worst case over the enumerated node set, and say which set. Separately and cheaply: either use `rnd` in build() or drop it, and stop citing the A_crit rows as "constant across ten seeds" — the construction ignores the seed, so that row is evidence of nothing.

## 28. [MAJOR] §3.1 Speed is not the problem, and the answer is useless anyway
*lens: methods*

**Quote.** With one ground per conclusion, a single compromised agent takes down a **median of half the collective** ... "About 52,000 of your conclusions no longer stand"

**Defect.** With one ground per conclusion the cascade is `any(p in dead)`, which is exactly forward reachability. The generator draws every edge from a lower index to a higher one (`c = randrange(1,N); p = randrange(c-200, c)`), so the descendant set of node t is essentially {t+1..N}. I measured it on the note's own graph: dead vs N-t for individual targets — 95,041 vs 95,086; 93,819 vs 93,895; 17,284 vs 17,343; median fallout 59,716 against median (N - t) of 59,798. The agreement is within 0.15%. The 'half' is the median of a uniform draw from a totally ordered index, not a property of dependency structure, and it would be 50% for any edge density above the percolation point.

**Attack.** Your first measured result is an identity: in an index-ordered DAG with mean in-degree 10, everything above the target dies, so the median loss over uniformly drawn targets is N/2 by arithmetic. It carries no information about warrant, ZTL, or decentralised collectives — you would get 52,000 from any random generator with the same ordering convention. Presenting it as 'the honest state of the mechanism' dresses a tautology as a negative finding.

**Correction.** Three edits, in `/media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md` and `/media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_swarm.py`. 1. FIX THE NUMBER FIRST (note line 123, probe docstring line 21). The probe prints 59,716 (59.7%), not 52,000. Either quote 59,716 or drop the figure. Leaving an unreproducible number under §7's "every number is printed by a program" is the cheapest possible hit to take. 2. DEMOTE §3.1's MAGNITUDE AND STATE THE IDENTITY (note lines 117-124). Replace "takes down a median of half the collective ... About 52,000 of your conclusions no longer stand" with something that concedes the construction, e.g.: > With one ground per conclusion the cascade is exactly forward reachability, and at mean in-degree 10 over a 200-wide backward window the reachable set from a target is its entire descendant set — measured at 99.9% of the nodes above it in the generator's index. The median fallout over uniformly drawn targets is therefore N/2 by construction, not by dependency structure. The finding here is qualitative and should be read as such: single-ground warrant has *no* containment, and the size of the answer is set by the graph rather than by anything the mechanism does. Also amend the abstract (lines 24-25) — "a single loss takes down half the collective" is the sentence that most needs the hedge, since it is what a reader carries away. 3. REPLACE THE TAUTOLOGY WITH THE MEASUREMENT THAT ISN'T ONE. The informative quantity is *where the containment fails*, and it costs one loop over `E` in the existing probe. Measured, N=100,000, window 200, median over the same 40 targets: | mean in-degree | median fallout | dead/(N−t) | |---|---|---| | 10.0 | 59,716 | 0.999 | | 5.0 | 59,208 | 0.991 | | 2.0 | 39,463 | 0.787 | | 1.2 | 4 | 0.000 | | 1.0 | 4 | 0.000 | That is a real statement about the mechanism: single-ground `any` semantics percolates above mean in-degree ≈2, the note's regime sits five times past it, and the DICE phase-3 numbers were never near the interesting part of the parameter space. It survives the hostile attack because the transition location is not fixed by the index convention. Report it beside the scale-free contrast already in hand (same density, median 12, worst 33,943) to show the magnitude is generator-dependent while the transition is the transferable finding. 4. Replace the vacuous guard at probe_swarm.py:121 (`assert med > N * 0.3`, unfailable in this generator) with one pinned to the transition — e.g. assert median fallout collapses below 1% of N at mean in-degree 1.2 and exceeds 30% at in-degree 2. Then §7's "115 stands" is not carrying a stand that cannot fail. Optional but worth it: §3.2's 0% column entries for random-local and hierarchy are also N−min(t) to within 0.05%. A one-line footnote saying the zero-redundancy column is the uncontained baseline and carries no structural content costs nothing and removes the follow-up attack.

## 29. [MAJOR] §3.6 An incomplete map, and a stale one (the gate)
*lens: methods*

**Quote.** Under delay and loss, a collective acting without a gate spends **about two thirds of its actions outside its own containment requirement** — 66% even at zero loss, because delay alone suffices

**Defect.** The causal clause is false and is contradicted by the probe's own table. In `probe_gate.mission` the ungated arm never reads `seen` at all: `if gated and c_pred + margin > LIMIT` short-circuits, so with gated=False the collective acts every step regardless of delay or loss (db/probe_gate.py:92-97). Violations = rate x (number of steps where the TRUE worst exceeds LIMIT), which is independent of the channel. The printed table shows it: 30% loss / delay 3 gives 2030 violations and 30% loss / delay 10 gives 2030 — identical. The 66% is 1 - t_cross/STEPS, where t_cross is when drift=20 per step first pushes 2% of N=20,000 onto one of 5 sources at STEPS=300. Change STEPS to 150 and it is ~33%; change LIMIT and it moves arbitrarily.

**Attack.** '66% even at zero loss, because delay alone suffices' asserts a mechanism your control arm cannot exhibit — the ungated collective has no observation channel to be delayed. The number is a ratio of two constants you chose (drift, LIMIT, N, STEPS) and is invariant to every variable the section is about. This is the load-bearing sentence for the §4 containment criterion and it does not survive reading the function.

**Correction.** Replace the paragraph at /media/vitaly/SSD_1000GB/Projects/ZTL/paper/warrant-containment-note.md:228-230, and the matching prose in probe_gate.py's "THE FIRST TABLE IS THE ARGUMENT" block (/media/vitaly/SSD_1000GB/Projects/ZTL/db/probe_gate.py:~140), with a version that separates the two facts: "An ungated collective acts unconditionally, so once drift pushes the true worst-case concentration past the limit — step 102 of 300 here — every remaining action is in violation: 66% of the run. That figure is a property of the run length and the drift rate, not of the channel. It is identical at every mean delay tested (1 to 1000) and at every loss rate including 100%, because with the gate off the collective's map never enters the decision at all. What the channel governs is not how much violation occurs but how much of it is *detectable*: at zero loss and mean delay 3 the collective's own delayed map is already over the limit at 195 of the 198 violating steps, so only 1% of the run is genuinely blind; at 50% loss and mean delay 10, 55% of violating steps are invisible in the map. Loss, not delay, is what makes a violation unreportable — under delay alone the blind window merely equals the update lag." Concretely: delete "Under delay and loss", delete "— 66% even at zero loss, because delay alone suffices —", and delete "and nothing reports it at the time" (contradicted at zero loss, where the map reports it 98.5% of the time). If the 66% is kept as motivation, state t_cross=102 and STEPS=300 inline so the horizon dependence is visible, and demote it from "THE ARGUMENT" — the argument for a gate is the blind-share figure, which is a genuine channel effect. Optionally add an assertion to probe_gate.py pinning ungated violations as invariant across mean_delay, so the causal claim cannot silently return.

## 30. [MAJOR] §3.6 An incomplete map, and a stale one (the gate)
*lens: methods*

**Quote.** A gate that acts only while a bound built from observables clears the limit holds violations at zero on three channels and **leaks on two** (270 and 170 violations). The margin used is therefore *not a sound bound*

**Defect.** Five configurations, one run each, no replication, and the leak pattern is non-monotone in the parameter that should drive it: 30% loss with mean delay 3 leaks 270, while 30% loss with mean delay 10 leaks 0. A longer delay produced a safer gate. Separately, the margin (db/probe_gate.py:88-91) uses `staleness = step - last_seen_change`, i.e. time since ANY update arrived — which is ~0 on almost every step because arrivals are near-continuous — rather than per-source confirmation age. Permanently lost updates accumulate a gap of order loss*drift*step/(SOURCES*N), which by step 300 at 30% loss exceeds LIMIT itself, while the margin stays pinned near mean_delay*drift/N. Table 3 ('maximum action rate') is also not measuring a rate: `rate` enters `mission` only as a multiplier on acted and violations and has no effect on the gate decision, so violations are either 0 for every rate or >0 for every rate; the printed '100' is just the top of the tested list. Table 3 also silently drops the (30% loss, delay 3) channel — the one that leaks.

**Attack.** Three-of-five versus two-of-five from single runs, with the leak going the wrong way in delay, is a coin flip reported as a regime map. And your diagnosis — 'the fix is a margin derived from a model of the channel rather than a larger constant' — mislocates an ordinary specification bug: you measure staleness against the arrival stream instead of against per-source confirmation, so the margin is structurally blind to exactly the lost updates the experiment introduced. A reviewer will read 'not a sound bound' as intellectual honesty and then discover the bound was never a candidate.

**Correction.** Four concrete changes, none of which require retracting the quoted sentence. 1. probe_gate.py:135 -- restore the dropped channel: change the Table 3 loop to the same five configs as lines 111/121, ((0.0,3),(0.1,3),(0.3,3),(0.3,10),(0.5,10)). It will print max rate 0 for (0.3,3), which is the honest output. 2. probe_gate.py Table 3 -- either delete it or make it non-degenerate. As written `rate` cannot change the gate decision, so "maximum action rate" is 100-or-0 by construction. If a rate ceiling is wanted, rate must enter the model (e.g. actions per step consume warrant, or the margin scales with rate x staleness); otherwise relabel the table "actions delivered under the gate" and drop the word "maximum rate", and delete the probe's claim "Where the bound holds, a high action rate is affordable" -- it is vacuous. 3. probe_gate.py:88-91 -- record, in the comment, the MEASURED fact that staleness == 0 on 300/300 steps at d=3 (max 1 at d=10), so the margin is in practice the constant mean_delay*(obs_rate+unseen_rate)/N. Tag it MEASURED. Note that the loss term is multiplied by a ~mean_delay-length window while lost updates accumulate over the whole elapsed run -- an understatement of order step/mean_delay (~100x). Optionally show the working repair as a commented variant: margin = ((staleness+mean_delay)*obs_rate + step*unseen_rate)/N, which gives 0 violations on all five channels at the cost of ~65% of the actions (1240 -> 440 at (0.3,3)). 4. paper/warrant-containment-note.md 3.6 -- add one sentence after "works in some regimes": the split is not a regime map. Say that the three clean channels are clean because a larger declared mean_delay enlarges the margin and trips the hold before c_act crosses the limit (measured: hold at step 85 vs crossing at 97 for (30%, d=10); hold at 124 vs crossing at 97 for (30%, d=3)) -- i.e. the successes are themselves bought by a bigger constant, which is the fix the note rejects. This strengthens rather than weakens "not a sound bound". Also worth adding, because it is free and favourable: the gate result IS replicated. Ten seeds give 10/10 leaks at (0.3,3) and (0.5,10) and 0/10 on the other three. Add that row to the 3.7 variance table (and to probe_variance.py, which currently omits probe_gate entirely) so the "one run each" objection cannot be raised at all. Do NOT adopt the reviewer's proposed fix: per-source confirmation age was implemented and tested and leaves the same two channels leaking (250 and 120 violations). Saying so explicitly in 3.6 pre-empts the objection.

## 31. [MAJOR] §3.6 (blindspot) and §6
*lens: prior-art*

**Quote.** When edges are missing from the observed graph, the error runs **one way only**: a missing edge can make two grounds look more independent than they are, never less.

**Defect.** This is not a property of incompleteness; it is a property of the one hiding operator the probe implements. `db/probe_blindspot.py:60-62` does not remove an edge — for an aliased agent it *substitutes* a fabricated second source: `seen_ev[c] = [("s", ev[c][0][1]), ("s", (ev[c][0][1] + 1) % SOURCES)]`. The observed graph is therefore not a subgraph of the true graph, so the prose ('edges are missing') does not describe the experiment. More seriously, the note's own §1 supports *alternative* grounds (`inv-17|inv-18`); a missing alternative makes the predicted cascade *larger* than the actual one — pessimistic, the direction the note says cannot occur. In provenance-polynomial terms (Green, Karvounarakis & Tannen, PODS 2007) a missing ⊗-factor and a missing ⊕-monomial move the label in opposite directions; monotonicity in one direction only holds if incompleteness is restricted to conjunctive/aliasing edges, which the note never states. §6 then generalises the artefact to a standing limitation: 'a measured blast radius can be understated and never overstated.'

**Attack.** Your one-directionality result is the strongest structural claim in §3.6, and it is an artefact of hiding only the edges that can flatter you. Hide an alternative ground — which your own ledger syntax supports — and the error reverses. Also, your code substitutes edges rather than deleting them, so the sentence 'edges are missing from the observed graph' does not describe what you ran. Either restrict the claim to conjunctive-dependency incompleteness and prove the monotonicity, or withdraw it.

**Correction.** Four changes; the first two are load-bearing. 1. Restrict the claim to the class actually tested, in §3.6. Replace: "When edges are missing from the observed graph, the error runs one way only: a missing edge can make two grounds look more independent than they are, never less." with something like: "The incompleteness measured here is of one class: a recorded identity between two named grounds — the fact that two names are one source — is missing from the map. For that class the error runs one way only: the map can make two grounds look more independent than they are, never less. The direction is a property of the class, not of incompleteness. A missing *alternative* ground (§1's `inv-17|inv-18`) runs the other way: the same model, hiding one of two cited grounds instead of an identity, predicts 0.206 against an actual 0.089 at full hiding — pessimistic by 0.116, larger than the optimistic error measured above. In provenance-semiring terms (Green, Karvounarakis & Tannen 2007) a missing ⊗-factor and a missing ⊕-monomial move the annotation in opposite directions." Add the counter-table as a row block or a footnote; it is a measured result and belongs in the corpus. It is also a seventh failed prediction for §5 if the author wants it there. 2. Fix §6 and `KNOWN-LIMITS.md`. The sentence "A dependence nobody recorded is invisible, so a measured blast radius can be understated and never overstated" is false as written under the note's own OR-grounds. Either: (a) narrow it — "an unrecorded *shared dependence* makes a blast radius understated; an unrecorded *alternative ground* makes it overstated, and the ledger cannot tell which kind it is missing"; or (b) state and defend the warrant principle that would rescue one-directionality — an uncited ground supplies no warrant, so only defeater-incompleteness bites — and then say so explicitly in §3.6, and note that C_actual there is a *world* quantity while the ceiling is a *warrant* quantity. Option (b) is the stronger paper but obliges §3.6 to say why the uncited alias counts against the system while an uncited alternative does not. §4's "the blindspot table says the error from incompleteness is one-directional" needs the same narrowing. 3. `db/probe_blindspot.py:58-63`. Either change the model so the observed graph is genuinely a subgraph — give the aliased agent's second name no recorded origin rather than a fabricated one — or, if the substitution is kept for simplicity, say in the docstring and in the note that what is hidden is a *coreference*, not an edge, and that "missing edge" is loose. I checked: the deletion form gives the same positive direction, so this is a cost-free fix to the description. 4. `db/probe_blindspot.py:159-165`. Reword the printed claim ("It is never negative, and cannot be… There is no symmetric case where an incomplete map flatters the danger instead of hiding it") — it is refuted by the variant above. Retitle the assert at line 160 to what it actually pins: the direction holds *for identity-hiding*, not for hiding in general. Consider adding the alternative-hiding variant to the probe as a second table so the assert set records both directions; that turns the weakness into a measurement, which is the corpus's own habit.

## 32. [MAJOR] §5a, β-factor comparison
*lens: prior-art*

**Quote.** design values for β in IEC 61508 run from **0.5% to 10%**, while §3.4 measures containment surviving hidden correlation up to **q\* ≈ 0.35**. A tolerance of 35% is an order of magnitude looser than what the engineering disciplines that build these systems consider acceptable.

**Defect.** Two problems. (i) The quantities are not commensurable. β is the *conditional fraction of component failures attributable to a common cause*; q in `db/probe_criterion.py:55-58` is the probability that a declared-redundant second ground is not genuinely added — a structural share of fake redundancy, not a failure-rate fraction. Equating them requires an argument the note does not make, so both the 'order of magnitude looser' remark and the self-criticism it carries are unearned. (ii) β is presented as the state of the art; in practice it was superseded precisely because it cannot handle k-of-n redundancy — the Multiple Greek Letter model, the Binomial Failure Rate model, and above all the α-factor model (Mosleh et al., NUREG/CR-5485, 1998) are the standard in nuclear PRA, and the α-factor model is the one that handles §3.5's 2-of-3 quorum. Also missing from the fault-tree citation: A_crit and the §1 'exposure' measure are structural importance measures (Birnbaum, 1969; Fussell–Vesely), and minimal-cut-set analysis is already a *non-probabilistic* computation of what still stands (Vesely et al., NUREG-0492 Fault Tree Handbook, 1981; Barlow & Proschan, 1975).

**Attack.** You compare your q* to β and conclude you are ten times looser than industry. You are not — they are different quantities and the comparison is a category error that happens to read as humility. Meanwhile you assert that CCF 'quantifies probability... whereas this work asks what a conclusion is still warranted by afterwards': minimal cut set analysis is purely combinatorial and answers exactly that, and has since 1981. And β has been superseded by α-factor for the k-of-n case you measure in §3.5.

**Correction.** Four edits to §5a (paper/warrant-containment-note.md). 1. Replace the identity claim (lines 325-327). Currently: "The β-factor is the fraction of failures arising from a single common cause — the same quantity this note calls hidden correlation `q`." Replace with an explicit bridge and its limit: "The β-factor splits a component's failure rate into an independent and a common-cause part, λ = (1−β)λ_ind + βλ_CCF. `db/probe_criterion.py:55-58` splits declared redundancy the same way — a second ground is added only with probability r(1−q) — so q plays β's structural role: the share of nominal redundancy defeated by a shared cause. It is not the same measurement. β is a share of failure *events*, estimated from operating data; q is a share of declared-redundant *pairs*, set by hand in a simulation. Nothing here calibrates one against the other." 2. Replace the "order of magnitude looser" passage (lines 333-338) — the comparison is mis-signed. q* is the point at which this model *breaks* (the smallest q with C ≥ 1%, `probe_criterion.py:155-158`), so larger q* means more robust; IEC 61508's 0.5–10% are design and estimation values achieved under defensive measures, not an acceptability ceiling. The honest version, which is still uncomfortable but true: "Design values for β in IEC 61508 run from 0.5% to 10%; §3.4 measures this model losing containment only at q* ≈ 0.35. Read correctly that is not a claim of tolerance but a confession of coarseness: the entire range in which real common-cause engineering happens sits an order of magnitude below this model's breaking point, so the whole 0.5%–10% band collapses into a single verdict — contained — and nothing in these runs discriminates a well-defended design from a poorly defended one. The instrument has no resolution where the discipline does its work." 3. Fix the state of the art. β-factor is the simplest CCF model and is known to be inadequate for k-of-n redundancy — precisely the case §3.5 measures. Add after the IEC 61508 sentence: "β is also the oldest of these models and the weakest: it cannot distinguish a 2-of-3 quorum from a 1-of-3 parallel set, because it assumes a common cause takes every redundant leg. The Multiple Greek Letter and Binomial Failure Rate models and, as the working standard in nuclear PRA, the α-factor model (Mosleh et al., NUREG/CR-5485, 1998) exist for that case — which is the case §3.5 reports. This note compares itself to the superseded model while measuring the configuration the superseded model cannot express." 4. Retract the FTA distinguishing clause (lines 340-344). Delete "it quantifies the *probability* that components fail together, whereas this work asks what a conclusion is still *warranted* by afterwards" and replace: "Minimal cut sets and their dual path sets are already a non-probabilistic computation of which combinations fail and which structure still stands (Vesely et al., NUREG-0492, 1981; Barlow & Proschan, 1975), and A_crit and the exposure measure of §1 belong to the family of structural importance measures (Birnbaum, 1969; Fussell–Vesely) rather than being new. What the CCF and fault-tree literature does not do, so far as I can find, is separate an authority dimension from an evidence one (§3.4), or treat the incompleteness of the dependency map itself as a measured quantity rather than a given tree (§3.6)." One reviewer claim to push back on: the finding calls β and q non-commensurable and the comparison a category error. That is too strong — r(1−q) in the probe is structurally the β decomposition, and the bridge is one sentence long. The defect is that the note never writes that sentence, and then compares a measured breakpoint against a design value as though the second were an acceptability limit.
