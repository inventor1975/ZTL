__pycache__/
*.pyc
lean/.lake/
blueprint/web/
OLD/
lean/BridgeGen.lean
tool/.groq_key
tool/.*_key
zrefuter_runs/

# LaTeX build artefacts
paper/*.log
paper/*.out
paper/*.aux
mindreef_topics.json
ztljudgenode.db

# ---------------------------------------------------------------------------
# DECIDED, not forgotten (2026-08-13). The blog essays are published on
# vitalyreznik.com and are the curator's outward-facing copy; a second copy on
# GitHub would only rot out of step with the live one. They are ignored here so
# that "untracked" cannot be misread as "somebody forgot to commit this" —
# which is exactly how `dilemmas/cogito.py` sat outside the repository for
# months and then turned CI red the day a stand was wired to it. An explicit
# decision belongs in the machine, not in someone's memory.
essays/
Reproduce-ZTL.docx

# Working material for the AI-introspection thread. Specimens, predictions and
# fork output — measured but not part of the corpus, and deliberately not
# shipped: db/_ground.py degrades to a no-op when this directory is absent.
lab/
LinkedIn/

# GUARD (2026-08-24): невыданный Novelty Test Protocol и его окружение — НЕ в
# публичный репо. Держать приватно (приватный репо / шифр-бэкап). Эта правка
# .gitignore НЕ коммитится, чтобы даже имя папки не светить публично.
novelty/
requirements.lock
Containerfile

# Разбор книги прибором — по решению куратора 2026-08-25 живёт в ПРИВАТНОМ репозитории
book/credit-ledger-KNIGA.md
IDEAS.md

# черновики форка при прогонах атом-стора
tool/introspect/*.tasks/
lake-manifest.json
lean/.lake/

# ЧУЖИЕ ДЕРЕВЬЯ — не наш код, коммитить как свой нельзя, но и валяться
# безымянно не должны (найдено 2026-08-28 при осмотре хвостов).
#   prior-art/MAM  — downstream-memory-admissibility, разбор в ztl-private/MAM-D1..D6
#   prior-art/RAED — github.com/downstream-protocol/RAED @ 471f81d, проба в
#                    ztl-private/RAED-PROBE (собирается на Go 1.23)
# Наши находки по ним живут в ztl-private, не здесь.
prior-art/
atomstore_query.tasks/
DEPLOYED.json
inventory/feverous_data/
inventory/tau_airline/tasks/
