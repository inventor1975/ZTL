{
  "_about": "overlay for a typical hand-written DB wrapper: `$db->query(...)` is the sink, `$db->escape(...)` substitutes for the quoted SQL context. Method keys may be receiver-qualified: `$db->escape` and `$out->escape` are different acts.",
  "sinks": {
    "sql": {
      "methods": [
        "query",
        "query_row",
        "query_all"
      ]
    }
  },
  "sanitizers": {
    "methods": {
      "$db->escape": {
        "contexts": [
          "sql-quoted"
        ]
      },
      "$out->escape": {
        "contexts": [
          "html"
        ]
      }
    }
  }
}